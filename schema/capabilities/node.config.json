{
  "capability": "node.config",
  "version": 1,
  "fields": {
    "mode": {
      "required": true,
      "type": "string",
      "enum": ["normal", "maintenance"]
    },
    "remoteAccess": {
      "required": true,
      "type": "string",
      "enum": ["disabled", "enabled"]
    }
  },
  "crossFieldRules": []
}
