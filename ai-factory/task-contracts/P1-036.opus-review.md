# P1-036 — Opus adversarial cross-review

**Task:** Make the vendored safe-normalize Proxy guardian structurally fail-CLOSED via the direct
`node:util` import (un-fork the vendored copy from upstream).
**Risk class:** R1 (security-sensitive — gates ALL untrusted input into the on-device evaluator).
**Reviewer:** Opus 4.8 (independent cross-review of Codex-built `task/P1-036`).
**Date:** 2026-06-23.

## VERDICT: approve

The change is correct, structurally fail-closed, byte-identical to upstream (modulo header), and the
new parity test has real teeth (mutation-verified). No fail-open path remains. All acceptance tests
pass. Diff is confined to `os/` + `sdk/`.

## Verification performed (adversarial)

1. **Resolver gone / direct import present — PASS.**
   `grep` of the vendored `os/x86_64/ts-overlay/usr/lib/vita/ts/vita/safe-normalize.ts` finds NONE of
   `getBuiltinModule | resolveNodeIsProxy | isProxyObject | isNodeIsProxy | isObjectRecord | ?? false |
   GlobalWithNodeProcess | NodeIsProxy`. Line 2 is `import { types as nodeTypes } from "node:util";`.
   The two call sites (`safe-normalize.ts:114`, `:210`) now call `nodeTypes.isProxy(...)` directly.
   The whole `resolveNodeIsProxy()` / `?? false` block is deleted. No fail-open path remains.

2. **Vendored body byte-identical to upstream — PASS.**
   Stripping line 1 (`// Vendored from sdk/typescript/src/safe-normalize.ts`) from the vendored file
   yields a body byte-identical to `sdk/typescript/src/safe-normalize.ts` (10177 bytes each; `diff`
   reports no differences). The vendored header is EXACTLY the string the parity test strips (no
   `for P1-033.` suffix — confirmed via `cat -A`), so the import line (which is shared with upstream)
   is correctly compared, not stripped.

3. **Parity test is genuine and has teeth — PASS.**
   `os/.../safe-normalize-parity.test.ts` imports BOTH the vendored and the upstream `safeNormalize`,
   and asserts identical results over a corpus of all six required shapes:
   Proxy (reject), accessor-property (reject), Date (reject), Map (reject), plain object (accept),
   null-proto object (accept). It also asserts proxy traps and accessor getters are never invoked,
   and a second test asserts the byte-identical body. The full acceptance command:
   `node --experimental-strip-types --test sdk/typescript/test/safe-normalize.test.ts
   os/x86_64/ts-overlay/usr/lib/vita/ts/safe-normalize-parity.test.ts`
   → **9 pass / 0 fail** (parity = subtests 1–2, upstream suite = 3–9). Parity file alone: 2/2 pass.
   - **Mutation test:** replacing `nodeTypes.isProxy(value)` with `false` (simulating a fail-open
     regression) makes parity test 1 FAIL (`not ok`) — proving the test would catch a proxy slipping
     through. Restored clean afterward.
   - **Fail-closed proof:** a `Proxy` wrapping a *benign plain object* (which would otherwise be
     accepted) is rejected with reason `"Proxy objects are not accepted."`, while the same object
     unwrapped is accepted — confirming `util.types.isProxy` actually fires (`isProxy(p) === true`).

4. **Diff confinement — PASS (with note on the 4th file, see NOTES).**
   `git diff --name-only main...task/P1-036` returns exactly three files, all under `os/`:
   the vendored `safe-normalize.ts`, the new parity test, and `eval.test.ts`. No change outside
   `os/`/`sdk/`. `main.ts`, `eval.ts`, service/agentd/apply/diff paths UNTOUCHED. Upstream src AND
   upstream test UNCHANGED between main and branch (the P1-033 fork was only in the vendored copy, as
   the contract predicted). No `getBuiltinModule` remains anywhere in `os/`/`sdk/` except as a
   negative assertion string in `eval.test.ts:161`.

5. **`node:util` under pinned Deno `--no-remote` — NOTED (orchestrator host-verified).**
   The contract records that the orchestrator host-verified `node:util.types.isProxy` works under the
   pinned Deno with `--no-remote` (node: built-ins are allowed; not a remote import) and under Node.
   `deno` is not on this review shell; I rely on the recorded host verification per the contract.
   Node side independently confirmed here on v22.16.0.

## NOTES (non-blocking)

- **The 4th file `eval.test.ts` is a legitimate, necessary STRENGTHENING — not a weakened test.**
  The prompt said "no other vendored file / main.ts / service changed," and `eval.test.ts` is the one
  extra file in the diff. Its old assertion `assert.doesNotMatch(source, /from ["']node:/u)` is what
  *locked in* the P1-033 fork (it forbade any static `node:` import, forcing the indirect resolver).
  With P1-036's direct import, that assertion would now FAIL, so it had to change. The replacement
  asserts the NEW invariant: a direct `import { types as nodeTypes } from "node:util"` IS present, and
  `getBuiltinModule` and `?? false` are ABSENT — i.e. it now actively forbids the fail-open resolver.
  This is a tightening of the guardrail, fully aligned with the objective. `eval.test.ts` runs 4/4.
  Recommend the orchestrator note this file as in-scope when integrating (it is the inverse-test that
  necessarily flips with the implementation).

## Remaining orchestrator-owned gate (per contract DoD)

- Host-verify `wsl-verify.sh ts` still PASS (runtime works with the direct import; VITA-EVAL /
  VITA-PREVIEW present) and `deno check` clean. Not runnable from this review shell; flagged for the
  orchestrator's host run before final merge.
