# P1-037 — Opus adversarial cross-review

**Task:** On-device config-change EXPLAIN — render a `TransactionPlanChange` into human-readable
lines (`VITA-EXPLAIN` marker).
**Branch:** `task/P1-037` (481eac7)  •  **Risk class:** R1  •  **Reviewer:** Opus 4.8 (adversarial cross-review)
**Date:** 2026-06-23

## VERDICT: approve

Independently verified, clean, and merge-ready. No blocking findings.

---

## What was verified

### 1. Purity & determinism — PASS
- `explainTransactionPlanChange` is pure: no `fs`/`fetch`/`process`/`Date`/`Math.random`/`console`,
  no I/O of any kind (grep-confirmed across both upstream + vendored copies).
- Deterministic stable sort: emits **added → removed → changed**, each group sorted with the SAME
  `compareStrings` ASCII-codepoint comparator the diff uses
  (`sdk/typescript/src/transaction-plan-diff.ts`), so explain order matches diff order.
- Empty change → exactly `["no changes"]` (single sensible line). Verified by test + by code path
  (`lines.length === 0`).
- Does **not** throw on a frozen `TransactionPlanChange`: input arrays are copied before sorting
  (`[...capabilities].sort(...)` — `transaction-plan-explain.ts:24`), so no in-place mutation of a
  frozen array. Result is `Object.freeze`d.
- Determinism test runs 5 repeats and asserts both `deepEqual` and `JSON.stringify` equality.

### 2. Vendored ≡ upstream — PASS
- `diff` of upstream vs vendored bodies (stripping the 4-line `// Vendored from …` header) is
  **byte-identical** (exit 0). The import path is `./transaction-plan-diff.ts` in BOTH files (the
  vendored `transaction-plan-diff.ts` already sits as a sibling), so no import-path edit was even
  needed — the only delta is the header. Nothing else differs. Cleaner than the contract allowed.

### 3. Tests genuinely cover the surface — PASS
- Acceptance command run verbatim:
  `node --experimental-strip-types --test sdk/typescript/test/transaction-plan-explain.test.ts os/x86_64/ts-overlay/usr/lib/vita/ts/explain.test.ts`
  → **9/9 pass, 0 fail, 0 skipped.**
- Coverage confirmed: exact ordered lines for mixed/add-only/remove+change corpora; determinism
  (5 repeats); empty→`["no changes"]`; frozen edge names (`__proto__`, `constructor`, `""`);
  **upstream parity** (vendored ≡ upstream on a corpus); and a `main.ts`-wiring test asserting the
  exact `VITA-EXPLAIN: lines=N status=OK` + `VITA-EXPLAIN| …` marker shape.

### 4. main.ts wiring — PASS
- `explainTransactionPlanChange(change)` is computed from the **same `change`** variable that
  `VITA-PREVIEW` is emitted from (the real CURRENT→DESIRED diff), placed AFTER the PREVIEW marker
  and BEFORE the NOOP marker (`main.ts` runPreview).
- Prints `VITA-EXPLAIN: lines=<N> status=OK` then one `VITA-EXPLAIN| <line>` per line via `emit`
  (pure; no FS/network).
- ALL prior markers retained: `VITA-TS`, `VITA-EVAL`, `VITA-EVAL-REJECT`, `VITA-PREVIEW`,
  `VITA-PREVIEW-NOOP` (and `VITA-PREVIEW-ERROR`) all still present.
- Regression: eval + preview + diff tests → **33/33 pass.**

### 5. Diff hygiene — PASS
- Changed files confined to `os/` + `sdk/` (5 files, +361/-0). No evaluator/diff/safe-normalize/
  service/apply paths touched. No tests weakened, no `skip`/`only`/`todo` (grep matches were only
  substrings in corpus names like "add only").

## Notes (non-blocking)
- `deno check` (contract's orchestrator-side step) not run here — `deno` is not on PATH on this
  host. Node `--experimental-strip-types` executed both test files without type/strip errors, which
  covers typecheck-by-execution. Orchestrator should still run `deno check` + the QEMU boot-serial
  `VITA-EXPLAIN` confirmation as the contract's final orchestrator gate before closing DoD.
- The DoD's host-verify boot-serial check is orchestrator-owned and out of scope for this code
  review; nothing in the diff blocks it.

**Recommendation:** Merge to `main`.
