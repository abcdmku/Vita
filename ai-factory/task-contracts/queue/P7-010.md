---
id: P7-010
title: Agent capability wiring round 8 — register accounts in agentd
status: ready
phase: 7
pod: go-agent
risk_class: R3
fr: [FR-004, FR-007]
depends_on: [P7-008, P7-009]
target_paths:
  - agent
acceptance_command: "node tools/build/go-in-docker.mjs --dir agent test ./..."
allowed_network: false
budget_minutes: 40
artifacts:
  - agent/cmd/agentd/main.go
  - agent/transport/server.go
  - agent/transport/server_test.go
---

## Objective
Register the `accounts` capability (P7-009) into `agentd`, exactly like P7-008 wired services. Construct it
with its production facade in `agent/cmd/agentd/main.go` and add it to the registry; add its request decoder
to `DefaultRequestDecoders()` in `agent/transport/server.go`. After this, all THIRTEEN capabilities are
discoverable via `GET /operations`, applicable via `POST /apply`, readable via `GET /read/{capability}`, and
included in `GET /state`.

## User value
Completes the agent's exposed operation set — the managed-accounts config becomes a managed + readable
operation (FR-004/007).

## Non-goals
- No new capabilities, no transport behavior changes beyond registration + decoder wiring. No real useradd.

## Dependencies
P7-008 (wiring pattern + the existing 12-cap registration), P7-009 (accounts). Both merged.

## Acceptance tests
`acceptance_command` (go test in golang:1.26) + orchestrator `go vet`. Extend the transport integration
test: `GET /operations` returns all THIRTEEN registered names (sorted); `POST /apply` routes a plan to an
accounts op and commits (injected facade); `GET /read/accounts` returns the config; `GET /state` includes
accounts. accounts must have a registered decoder.

## Performance & security constraints
TCB. Loopback-only + fail-closed /apply unchanged. Production facade wired. Every registered capability MUST
have a decoder. `go vet` clean. R3 → reviewer-gated.

## Hardware profiles
n/a.

## Allowed tools & network
golang:1.26 via `tools/build/go-in-docker.mjs`. Offline. httptest.

## Required artifacts
- updated `agent/cmd/agentd/main.go` (+accounts), `agent/transport/server.go` (+1 decoder),
  `agent/transport/server_test.go` (integration covers all 13).

## Rollback plan
Revert branch `task/P7-010`.

## Definition of done
- [ ] `go test ./...` + `go vet ./...` clean in the container.
- [ ] /operations lists all 13 caps (sorted); /apply routes to accounts; /read/accounts + /state include it.
- [ ] accounts has a registered decoder. Diff within `agent/`.
