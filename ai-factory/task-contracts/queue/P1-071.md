---
id: P1-071
title: Catalog-driven install resolution (verified catalog entry → mirror-resolved → install plan) — VITA-INSTALL-RESOLVE [Phase 4]
status: ready
phase: 1
pod: package-platform
risk_class: R2
fr: [FR-010]
depends_on: [P1-063, P1-064]
target_paths:
  - packages/install
  - packages/catalog
acceptance_command: "node --experimental-strip-types --test \"packages/install/test/*.test.ts\""
allowed_network: false
budget_minutes: 90
artifacts:
  - packages/install/src/resolve-from-catalog.ts
  - packages/install/test/resolve-from-catalog.test.ts
---

## Objective
Phase 4 supply-chain join: given a VERIFIED signed catalog (P1-063 `verifyCatalog`) + an app id + version, resolve a
complete, fail-closed INSTALL PLAN — the capsule package ref + its mirror-resolved dependency closure (P1-064
`resolveFromMirror`, byte-verified SRI) + the capability grants — reusing the existing `resolveInstallPlan`
(packages/install/src/resolve.ts). This is the pure TS resolver that turns "install app X" into a verified, deterministic
plan an agentd slice can later apply. NO host mutation, NO download side effects (pure over caller-supplied catalog +
mirror store).

Concretely: a pure `resolveFromCatalog({catalog, appId, version, mirrorStore})`:
1. `verifyCatalog(catalog)` FIRST — if the catalog signature/integrity doesn't verify, REJECT (no resolution from an
   unverified catalog). 2. Locate the app+version entry (fail-closed if absent/ambiguous). 3. `resolveFromMirror` the
   entry's package + dep closure against the mirror store, byte-verifying every SRI (reject NOT_IN_MIRROR /
   INTEGRITY_MISMATCH). 4. Build the ordered install plan + capability grants via the existing `resolveInstallPlan`.
   Output is deterministic (byte-identical for the same inputs) + never throws at the boundary (safeNormalize the inputs).
Errors: CATALOG_UNVERIFIED, APP_NOT_FOUND, VERSION_NOT_FOUND, MIRROR_* (from P1-064), POLICY_REJECTED. No remote fetch.

## User value
The supply chain end-to-end as pure logic: a signed catalog + a mirror yield a verified install plan for a named app —
the foundation for `vita install <app>` (CLI P1-070) + a future agentd install slice. A tampered catalog, a missing/
bad-integrity dependency, or an unknown app all fail closed before any plan is produced.

## Non-goals
NO actual install/host mutation (a later agentd R3 slice applies the plan). NO network/downloader (the mirror store is
caller-supplied/in-memory, per P1-064). NO loosening of verifyCatalog or resolveFromMirror or validateLockfilePolicy
(reuse, don't re-implement/weaken). NO on-device marker (pure host-side module — SDK/unit-level floor).

## Dependencies
P1-063 (verifyCatalog + the catalog manifest types) + P1-064 (resolveFromMirror + SRI byte-verify) merged. Reuse
`packages/install/src/resolve.ts` (resolveInstallPlan), the catalog types from packages/catalog, and `safeNormalize`.
Read those before coding; match the package-platform test style (node --experimental-strip-types --test).

## Acceptance tests
`acceptance_command` — node TS tests: resolveFromCatalog produces the correct ordered plan + grants for a valid
verified-catalog app; REJECTS an unverified/tampered catalog (CATALOG_UNVERIFIED) before any resolution; REJECTS an
unknown app/version; REJECTS when a dependency is NOT_IN_MIRROR or fails SRI (INTEGRITY_MISMATCH); is deterministic
(byte-identical output under input key reorder); never throws on hostile input (cyclic/accessor/__proto__). `npm run
typecheck` clean on touched files.

## Performance & security constraints
Verify-catalog-FIRST (no resolution from an unverified catalog); byte-verify every dependency SRI (reuse P1-064); pure +
deterministic; fail-CLOSED on every invalid/ambiguous input; never throws at the trust boundary; no remote imports/fetch;
no lifecycle scripts; no keys/secrets. R2 → reviewer gate.

## Hardware profiles
Host-side (any); no on-device component.

## Allowed tools & network
Offline; Node ≥22 built-ins only (node:test/assert/crypto). No network/filesystem downloader.

## Required artifacts
`resolve-from-catalog.ts` (the pure resolver) + `resolve-from-catalog.test.ts` (the cases above). Two additive files
under packages/install; nothing else imported yet (low blast radius).

## Rollback plan
Revert branch `task/P1-071`.

## Definition of done
- [ ] node TS tests: valid resolve; unverified-catalog reject; unknown app/version reject; missing/bad-SRI-dep reject;
      deterministic; never-throws-on-hostile-input. typecheck clean.
- [ ] Reuses verifyCatalog + resolveFromMirror + resolveInstallPlan WITHOUT re-implementing or loosening them.
- [ ] Pure, fail-closed, no remote fetch, no keys.

## REVISION 2 (Codex R2 review: REVISE — 2 blocking — 2026-06-24)
1. **packageRef must reference the capsule ARTIFACT, not a metadata envelope.** resolve-from-catalog.ts (:124/:141/:162/
   :166) treats `packageRef` as a mirror key for a `{entry, lockfile}` JSON envelope and signs/verifies THAT envelope —
   but the catalog contract defines `packageRef` as the capsule/package artifact reference that downstream install
   fetches + integrity-verifies. FIX: resolve + verify the actual capsule ARTIFACT (its bytes/digest), and return the
   artifact's ref + integrity so the produced plan is a valid install plan for the real capsule package (not the metadata
   envelope).
2. **Fail-closed on artifact/catalog id+version mismatch.** (:159-174) the resolver never checks that the package
   contract embedded in the resolved artifact MATCHES the verified catalog selection. A signed catalog entry for
   appId=com.vita.notes@1.2.3 pointing at an artifact whose contract is com.other.app@9.9.9 currently returns ok=true with
   the catalog's appId/version but the artifact's package/grants. FIX: after resolving the artifact, verify its embedded
   package contract's appId + version EQUAL the verified catalog selection; REJECT fail-closed on any mismatch (never
   return an install plan where "install app X version Y" yields a different app/version).
Verify: `npm test` (resolve-from-catalog) — artifact-ref plan correct + id/version-mismatch rejected fail-closed.

## REVISION 3 (Codex R2: REVISE — packageRef-as-envelope recurs + proto-pollution — 2026-06-24)
The worker AGAIN treats `packageRef` as a UTF-8 JSON `{manifest, lockfile}` envelope; `parsePackageArtifact` requires JSON
+ embedded metadata so a REAL `file://...tar.zst` capsule cannot resolve. THE CORRECT MODEL (be explicit): the SIGNED
catalog entry carries appId, version, `packageRef` = the capsule ARTIFACT URI (e.g. `file://...tar.zst` — the SAME ref the
EXISTING `capsule.fetch` path P1-045 fetches + SRI-verifies), and `integrity` = that artifact's SRI digest. FIX:
1. resolve-from-catalog: verify the catalog entry SIGNATURE, then return an install plan carrying the catalog's signed
   appId+version + the artifact's `packageRef`+`integrity` so DOWNSTREAM install fetches+SRI-verifies the real capsule
   bundle. Do NOT parse the artifact as JSON — `parsePackageArtifact` must accept the opaque capsule bundle, NOT require a
   JSON metadata envelope.
2. appId/version MISMATCH (fail-closed): read the capsule's manifest.json from INSIDE the verified capsule bundle (the same
   manifest the runtime validates) and assert its appId+version EQUAL the catalog's signed appId+version; REJECT on any
   mismatch.
3. Proto-pollution: `catalogEntryCandidate` must build with a NULL-PROTOTYPE object + `Object.defineProperty` (not `{}` +
   assignment), so an own `__proto__` field in the artifact manifest cannot evade `validateCatalogEntry` via prototype
   mutation; add a nested-`__proto__` regression test.
Verify: `npm test` — a real tar.zst capsule ref resolves to a valid install plan; id/version-mismatch rejected; __proto__
evasion rejected.

## REVISION 4 (Codex R2: REVISE — capsule manifest SHAPE wrong, 3rd rev same class — 2026-06-24)
The worker keeps assuming the capsule bundle's `manifest.json` is a `CatalogEntry`/install-entry (top-level `package` +
`requestedCapabilities`). IT IS NOT. A real runtime capsule `manifest.json` is the agent **ExecutionManifest** shape:
`id`, `version`, `packageClass`, `runtime`, `resourceLimits`, etc. (the EXACT shape the capsule runtime validates — read
the ExecutionManifest type in agent/ to confirm fields; it is what the EXISTING capsule.execute/fetch path consumes). FIX:
1. `parsePackageArtifact` reads the capsule bundle's manifest.json as an **ExecutionManifest** (NOT a CatalogEntry); the
   bundle is a real P1-045 `file://...tar.zst`.
2. appId/version cross-check: assert the ExecutionManifest's `id` (== appId) and `version` EQUAL the catalog entry's
   SIGNED appId+version; REJECT fail-closed on mismatch. (The grants/capabilities come from the catalog entry's signed
   metadata, NOT from the capsule manifest.)
3. Fix the test fixture (resolve-from-catalog.test.ts ~:310): the "real capsule" tar.zst's manifest.json must contain a
   REAL ExecutionManifest (id/version/packageClass/runtime/resourceLimits), NOT `validInstallEntry()` — otherwise the
   "real tar.zst resolves" test is fake. Add the mismatch + __proto__ regressions (kept from rev3).
Verify: `npm test` — a real ExecutionManifest-bearing tar.zst resolves to a valid install plan; id/version mismatch + proto
evasion rejected fail-closed.

## REVISION 5 (Codex R2: REVISE — validation rigor; shape now correct, converging — 2026-06-24)
rev4 reads the ExecutionManifest correctly (good). Now the resolver re-validates it too LOOSELY vs the agent's REAL
validator, so it can produce a plan for a capsule the runtime later rejects. Match the agent's ExecutionManifest
validation exactly (read the Go validator in agent/ and mirror its rules) — fail-closed:
1. `readExecutionRuntime`: do NOT accept any non-typescript runtime blindly. Validate per packageClass: an `oci-service`
   requires `runtime.oci.image.digest` + `entrypoint`; a WASM class requires its runtime fields; etc. — exactly what the
   Go ExecutionManifest requires.
2. `validateOptionalExecutionObjects`: validate the real sub-shapes (reject `network:{}`, `lifecyclePolicy:{onUnhealthy:
   "bad"}`, malformed `healthChecks` entries) — match the agent validator, not just "is object"/"is array".
3. Bundle realism: require zstd (capsule.fetch ALWAYS zstd-decodes — reject raw tar); read ROOT `manifest.json` only
   (reject `nested/manifest.json`) — match P1-045 capsule.fetch exactly.
4. `verifySelectedManifest`: require manifest.integrity == the signed catalog artifact integrity AND manifest.packageClass
   == the signed package contract used in the returned plan (not just id+version). Mismatch => fail-closed.
Verify: `npm test` — invalid runtime/sub-shape/raw-tar/nested-manifest/integrity-mismatch/packageClass-mismatch all
rejected fail-closed; a valid P1-045 zstd capsule with a complete ExecutionManifest resolves.

## REVISION 6 (Opus R2 review: REVISE — converging, 3 specific items — 2026-06-24)
rev5 fixed points 1-4, 6 + the packageClass half of 5. 3 remaining:
1. **integrity-equality (point 5 completion):** `verifySelectedManifest` must ALSO require `manifest.integrity` ==
   the signed catalog artifact integrity (mirror the Go agentd `validateManifestMatchesRequest`: rejects when
   `manifest.Integrity != desired.Integrity`). Without it, resolve produces plans the execute path later rejects.
   Fail-closed on integrity mismatch.
2. **decompression-bomb guard:** `zstdDecompressSync` is unbounded — add a sane max decompressed-size cap and reject a
   bundle that exceeds it (DoS guard), before reading manifest/files.
3. **IPv6 CIDR canonicalization:** the network-grant CIDR validation must canonicalize/validate IPv6 host bits to match
   the Go validator (zero host bits / reject non-canonical), not just IPv4.
Verify: `npm test` — integrity-mismatch rejected; oversized-decompression rejected; non-canonical IPv6 CIDR rejected; the
valid path still resolves. (Use an Opus review, not `npm run review` — its same-commit cache returned a stale verdict.)

## REVISION 7 (Opus R2: REVISE — ONE item left; worker missed it twice — 2026-06-24)
rev6 closed items 2 (zstd cap) + 3 (IPv6 CIDR). ONLY ITEM 1 REMAINS and the worker has skipped it in rev5 AND rev6 — do
it explicitly now: in `verifySelectedManifest` (resolve-from-catalog.ts), ADD a check that the ExecutionManifest's
`integrity` field EQUALS the SIGNED catalog entry's artifact integrity (`entry.integrity`) — `if (manifest.integrity !==
entry.integrity) -> reject fail-closed`. This mirrors the Go agentd `validateManifestMatchesRequest` (`manifest.Integrity
!= desired.Integrity` => reject); without it, resolve emits plans agentd rejects at execute. The current id/version/
packageClass checks are NOT enough.
ALSO FIX THE FIXTURES (the reviewer proved adding the check breaks 3 valid-path tests because their fixtures don't set a
matching integrity): in the valid-path tests, set the capsule's ExecutionManifest `integrity` to EQUAL the catalog
entry's artifact integrity so the valid path passes. Add a REAL integrity-mismatch regression: manifest.integrity !=
entry.integrity => rejected (distinct from the artifact-bytes SRI check, which is a separate gate). Do NOT weaken any
test. Verify: `npm test` — valid path passes, manifest/catalog integrity-mismatch rejected fail-closed.

## REVISION 8 (Opus R2: REVISE — integrity CHECK correct; only the 3 fixtures need updating — 2026-06-24)
rev7 added the CORRECT `manifest.integrity !== entry.integrity` check in verifySelectedManifest + a real mismatch
regression — DO NOT change that logic. The ONLY remaining failure: 3 valid-path tests now fail because their fixtures'
ExecutionManifest integrity != the catalog entry's artifact integrity:
- resolve-from-catalog.test.ts: the valid-resolve test (#1), the mirror-dep-rejection test (#17), and the determinism
  test (#18).
FIX (fixtures ONLY): in those 3 tests, set the capsule ExecutionManifest's `integrity` field to EQUAL the catalog entry's
artifact `integrity` (the correct real-world invariant — the signed catalog entry's artifact integrity and the manifest's
declared integrity are the same value). Compute/set them consistently so the valid path passes. Do NOT touch
verifySelectedManifest's check or weaken any assertion. Verify: `npm test` — ALL pass (valid path + the mismatch +
proto + bomb + IPv6 regressions).
