---
id: P9-017
title: Migrate update.plan end-to-end — bundleRefString + bundleVersionString (reuse sriIntegrity + forbiddenSchemePrefix)
status: ready
phase: 9
pod: typescript-sdk
risk_class: R3
fr: [FR-005, FR-007]
depends_on: [P9-016]
target_paths:
  - schema/capabilities/update.json
  - schema/capabilities/conformance/update.json
  - schema/capabilities/formats
  - sdk/typescript/src/capability-manifest.ts
  - agent/internal/capmanifest/capmanifest.go
  - sdk/typescript/src/generated
  - sdk/typescript/test
  - agent/internal/capmanifest
  - runtime/evaluator
acceptance_command: "cd agent && go test ./internal/capmanifest/... && cd .. && node --experimental-strip-types --test sdk/typescript/test/update-conformance.test.ts sdk/typescript/test/capability-registry.test.ts runtime/evaluator/test/evaluate.test.ts"
allowed_network: false
budget_minutes: 80
artifacts:
  - schema/capabilities/update.json
  - schema/capabilities/conformance/update.json
  - sdk/typescript/src/generated/capability-manifests.generated.ts
---

## Objective
Migrate the `update` capability (op `update.plan` — verify the exact `Name`) end-to-end. The REAL entrypoint
`transport.DecodeJSONRequest[update.ApplyRequest]` + `Validate()` is the ORACLE. Read
`agent/capabilities/update/update.go` and match `validatePlan`/`validateBundleRef` EXACTLY (AGENTS.md
manifest≡agent + the 8 traps). This cap is CLEANER than capsule — NO array, NO uniqueBy, **NO secret scanner**.
- **`schema/capabilities/update.json` = the full request** `{ desired: { object: { targetSlot: enum[a,b];
  bundle: object { ref: bundleRefString; integrity: sriIntegrity; version: bundleVersionString } } } }`. All
  fields required.
- **REUSE existing formats**: `sriIntegrity` (P9-016 — verify update's `isValidSRI` is the SAME: sriPattern +
  base64 std/raw to 32/48/64; it is) and `forbiddenSchemePrefix` (P9-016 — update's `hasInlineBundleScheme`
  uses the SAME `{data,inline,literal}` scheme set; confirm and reuse).
- **NEW FORMATS (structured, NO regex engine; match the agent regexes EXACTLY via FROZEN corpora; ground-truth
  cross-check vs update.go):**
  - **`bundleRefString`** = `bundleRefPattern` `^[A-Za-z0-9][A-Za-z0-9._:@/-]{0,255}$`. The `ref` field ALSO
    applies length ≤ `maxBundleRefLength` (`maxBytes`) AND `forbiddenSchemePrefix` (`hasInlineBundleScheme`) —
    but NOT any secret scanner (the agent's `validateBundleRef` does NOT call a `containsInline*Material` on
    ref; verify). 
  - **`bundleVersionString`** = `bundleVersionPattern` `^[A-Za-z0-9][A-Za-z0-9.+_-]{0,127}$` + `maxBytes` ≤
    `maxBundleVersionBytes`. The `version` field applies ONLY this regex + length — **NO secret scanner, NO
    forbiddenSchemePrefix** (the agent's version check is regex+length only; do NOT add screens it lacks =
    AGENTS trap, STRICTER). (Pattern equals capsuleVersion's but the SCREENS differ — model it as its own
    format/field with no screens.)
- **Conformance ≡ agent**: `schema/capabilities/conformance/update.json` (≥ 22 full-request vectors) run by
  BOTH the manifest validator AND `DecodeJSONRequest[update.ApplyRequest]`+`Validate()`, asserting agreement.
  Cover: valid a/b slots, bad slot, valid ref (with `:@/.` chars), ref length boundary (255/256), ref with a
  forbidden scheme prefix (`data:`/`inline:`/`literal:` ⇒ reject) AND a benign `:`/`@`/`/` ref (accept), each
  SRI algo + padded/raw + wrong length, version boundary (127/128), a long-base64-looking version that the
  agent ACCEPTS (no secret scanner on version ⇒ accept — a REVEALING vector that fails if you wrongly add a
  screen), absent each field, dup JSON key, unknown field. The REAL entrypoint is the oracle.
- **Register** update (regenerate; orchestrator updates the registry test) + evaluator coverage. Agent UNCHANGED.

## User value
7th migrated cap; proves format REUSE (sriIntegrity + forbiddenSchemePrefix) across caps + a clean
no-scanner cap. The evaluator now covers OS updates.

## Non-goals
- No agent cap change. No other caps. Do NOT add a secret scanner to ref/version (the agent has none there) —
  matching the agent EXACTLY (not stricter).

## Dependencies
P9-016 (sriIntegrity + forbiddenSchemePrefix). Merged.

## Acceptance tests
`acceptance_command` green; orchestrator ALSO runs full container `go test ./...` + `npm run typecheck` + full
TS suite + a ground-truth Go cross-check (bundleRefString/bundleVersionString ≡ update.go regexes on a battery).
manifest≡agent via the real entrypoint on every vector.

## Performance & security constraints
Deterministic, fail-closed; structured formats byte-identical TS≡Go + ≡ the agent regexes; BYTE lengths; NO
extra screens on ref/version. Strict TS / clean `go vet`. R3 → reviewer-gated.

## Hardware profiles
n/a.

## Allowed tools & network
Offline. Node + native `go`.

## Required artifacts
- `update.json` (registered) + bundleRefString + bundleVersionString + REUSED sriIntegrity/forbiddenSchemePrefix
  + conformance (TS + Go, real entrypoint) + regenerated const + evaluator coverage.

## Rollback plan
Revert / discard.

## Definition of done
- [ ] `acceptance_command` green + full container `go test ./...` + `npm run typecheck` + full TS suite.
- [ ] update.json models the full request; bundleRefString/bundleVersionString match the agent EXACTLY (ground-truth cross-checked, NO extra screens); sriIntegrity + forbiddenSchemePrefix reused; conformance ≡ agent at the real entrypoint.
- [ ] update registered (7th cap); evaluator covers it; agent unchanged. Diff within listed paths.
