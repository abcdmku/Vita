---
id: P1-014
title: x86_64 disk image layout — A/B roots + recovery + RAUC bundle plan (deterministic scaffold)
status: ready
phase: 1
pod: os-image
risk_class: R2
fr: [FR-023, FR-001]
depends_on: [P1-011, P1-012]
target_paths:
  - os
acceptance_command: "node --experimental-strip-types --test os/x86_64/test/image-layout.test.ts"
allowed_network: false
budget_minutes: 85
artifacts:
  - os/x86_64/image-layout.mjs
  - os/x86_64/image.conf
  - os/x86_64/test/image-layout.test.ts
---

## Objective
The **A/B disk image layout PLAN** (spec §6 RAUC A/B, ADR-0005; Phase 1): `planImageLayout()` assembles
the GPT partition layout — ESP, root-A, root-B, recovery, and a data partition — plus the RAUC slot
mapping and a RAUC update-bundle manifest plan, all deterministic + offline (like P1-011/P1-012, NOT a
real image build). Consumes the UKI (P1-012) output as the boot artifact for each root slot.
- `os/x86_64/image.conf`: pinned partition sizes/types/GUIDs (fixed, deterministic), slot mapping
  (A/B + recovery), RAUC compatible string. No eval-time network.
- `os/x86_64/image-layout.mjs`: `planImageLayout()` (pure) returning the normalized byte-deterministic
  layout + RAUC slot/bundle plan; references the UKI + root-image outputs as digest-pinned/unresolved
  external inputs (reuse P1-012's external-precondition + canonical-path model — inputs unresolved until
  the upstream build supplies real digests; unresolved blocks assembly). SOURCE_DATE_EPOCH set.
- `os/x86_64/test/image-layout.test.ts`: asserts the layout is byte-deterministic; A/B+recovery slots
  present + non-overlapping; sizes/GUIDs pinned; RAUC slot mapping coherent; external inputs are
  digest-verified preconditions (sentinels rejected); all container/host paths canonical (reject `..`).

## User value
The partition/slot/bundle plan the installer + RAUC updater (FR-023) build on — the structure for
atomic A/B updates with rollback, the core Phase-1 reliability property.

## Non-goals
- No real partitioning/image build/RAUC run (needs a privileged Linux host — later). Plan + determinism
  test only. No dm-verity yet (a following contract). Do not modify P1-011/P1-012 files beyond reference.

## Dependencies
P1-011 (root image), P1-012 (UKI) — merged. Reuse their external-precondition + canonical-path helpers.

## Acceptance tests
`acceptance_command` — `image-layout.test.ts` passes (determinism; non-overlapping A/B+recovery; pinned
sizes/GUIDs; coherent RAUC mapping; digest-verified external inputs w/ sentinel rejection; canonical
paths). Orchestrator also runs `npm run typecheck`.

## Performance & security constraints
Deterministic/pure (spec §8.2). Digest-pinned/unresolved external inputs; canonical-path validation
(reject traversal). No production keys. Strict TS (no `any`). R2 → reviewer-gated.

## Hardware profiles
x86_64 (this contract); rpi5 later.

## Allowed tools & network
Offline. Node only for plan/test.

## Required artifacts
- `os/x86_64/image-layout.mjs`, `os/x86_64/image.conf`, `os/x86_64/test/image-layout.test.ts`.

## Rollback plan
Revert branch `task/P1-014`.

## Definition of done
- [ ] Acceptance passes; `npm run typecheck` clean (orchestrator-run).
- [ ] A/B + recovery slots non-overlapping + pinned; RAUC slot/bundle mapping coherent; UKI consumed per slot.
- [ ] External inputs are digest-verified preconditions (sentinels rejected); paths canonical (no traversal).
- [ ] Diff within `os/`.
