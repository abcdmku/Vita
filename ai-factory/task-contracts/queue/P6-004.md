---
id: P6-004
title: Agent capsule capability — transactional installed-capsule registry, fail-closed
status: ready
phase: 6
pod: go-agent
risk_class: R3
fr: [FR-007, FR-012]
depends_on: [P1-006, P1-007]
target_paths:
  - agent
acceptance_command: "node tools/build/go-in-docker.mjs --dir agent test ./..."
allowed_network: false
budget_minutes: 70
artifacts:
  - agent/capabilities/capsule/capsule.go
  - agent/capabilities/capsule/capsule_test.go
---

## Objective
A transactional capability `agent/capabilities/capsule/` that stores the node's **installed-capsule
registry** (spec §13 capsules) to a FIXED agent state path, applied atomically with rollback — the proven
nodeconfig (P1-007) pattern. A typed `Registry` = a list of `CapsuleEntry` (id + version + integrity
(SRI) + state: "installed"|"disabled"). `Handle` reads current; `Apply` validates a desired registry and
writes via a single atomic commit point (temp + rename — nothing fallible after the rename), returning a
restoring `Undo`; commit + rollback proven through the engine. Implements `Validate()` for the transport
gate (with duplicate-JSON-key rejection + canonical persisted bytes, per AGENTS.md). Injected FS facade.
- **Validate fail-closed**: capsule id well-formed (reverse-DNS or opaque id); version non-empty;
  integrity a real SRI (sha256/384/512); state from the closed set; reject DUPLICATE capsule ids; reject
  embedded capsule bytes (reference + integrity only, §13.1). Absent required field ⇒ reject (not
  defaulted). No `os/exec`, no real capsule install.

## User value
The on-host record of which capsules are installed + their integrity-pinned versions (FR-012, §13),
transactionally — the controller can read it (via /read) + stage changes. Real capsule install/run is a
later host contract.

## Non-goals
- No real capsule download/install/run/snapshot, no network. Store + validate the registry only. No
  `os/exec`. No `cmd/agentd/main.go` edits (wiring is a follow-up). No capsule byte storage (reference only).

## Dependencies
P1-006 (engine), P1-007 (atomic-file pattern). Both merged. Follow the AGENTS.md dup-key + canonical-bytes rule.

## Acceptance tests
`acceptance_command` (go test in golang:1.26) + orchestrator `go vet`. Cover: Handle reads (canonical
bytes); Apply writes + restoring Undo; transaction commit; rollback (later op fails ⇒ prior restored);
malformed id/version/SRI/state rejected; duplicate capsule ids rejected; embedded capsule bytes rejected
(§13.1); duplicate JSON keys rejected; absent required field rejected; atomic-write-failure leaves the
live file unchanged (single commit point). Injected FS.

## Performance & security constraints
TCB. Single atomic commit point (no error path after rename). Fixed state path. Fail-closed (reject
incomplete/missing/duplicate; canonical persisted bytes; dup-JSON-key rejection). §13.1. No `os/exec`.
`go vet` clean. R3 → reviewer-gated.

## Hardware profiles
n/a (file I/O via injected FS; capsule install is a later host contract).

## Allowed tools & network
golang:1.26 via `tools/build/go-in-docker.mjs`. Offline.

## Required artifacts
- `agent/capabilities/capsule/{capsule.go,capsule_test.go}`.

## Reviewer revision (round 2 — BLOCKING, from P6-004.review.md)
1) **The absent≠zero rule applies to the TYPED path, not just JSON.** `Registry.Capsules` is a non-pointer
   slice and `normalizeRegistry` treats `nil` as explicit empty, so a directly-constructed
   `ApplyRequest{Desired:&Registry{}}` passes `Validate()`/`Apply()` and writes `{"capsules":[]}` (the JSON
   `UnmarshalJSON` rejects missing `capsules`, but the typed path is fail-OPEN). Use an explicit presence
   model (e.g. `Capsules *[]CapsuleEntry`): nil ⇒ reject "capsules is required"; non-nil empty `[]` ⇒
   accept (deny-all/empty registry). Make `Validate()` reject the nil case consistently. Regression for
   the typed-nil case.
2) **`version` must not smuggle inline content.** `validVersion`/`versionPattern` allows `:`, so
   `version:"data:QUJD"` passes and is persisted — embedded capsule bytes in an allowed scalar (§13.1).
   Constrain `version` to a safe charset that cannot carry an inline/`data:`/`literal:` scheme (reject
   `:`-schemes / data URIs / long base64 blobs). Add rejection regressions.

## Rollback plan
Revert branch `task/P6-004`.

## Definition of done
- [ ] `go test ./...` + `go vet ./...` clean in the container.
- [ ] Transactional store of a valid capsule registry; commit + rollback proven; single commit point.
- [ ] Fail-closed: id/version/SRI/state + duplicate-id + missing-field + dup-JSON-key + §13.1 rejected; canonical bytes; Validate().
- [ ] No `os/exec`, no `cmd/agentd/main.go` edits. Diff within `agent/capabilities/capsule/`.
