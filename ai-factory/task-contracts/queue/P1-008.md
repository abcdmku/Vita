---
id: P1-008
title: Agent local control transport — loopback HTTP: status, capabilities, transactional apply
status: ready
phase: 1
pod: go-agent
risk_class: R3
fr: [FR-006, FR-007, FR-008]
depends_on: [P1-004, P1-005, P1-006]
target_paths:
  - agent
acceptance_command: "node tools/build/go-in-docker.mjs --dir agent test ./..."
allowed_network: false
budget_minutes: 80
artifacts:
  - agent/transport/server.go
  - agent/transport/server_test.go
  - agent/cmd/agentd/main.go
---

## Objective
Give the agent a **local control surface** the controller can call (spec §7 controller↔agent, §8.2
apply path, FR-008 status). An `http.Handler` (`agent/transport/server.go`) exposing:
- `GET /healthz` — read-only status (from P1-004 status).
- `GET /capabilities` — discovered node capabilities (from P1-005), read-only.
- `POST /apply` — accept a typed plan (JSON body), **validate it fail-closed**, run it through the
  transaction engine (P1-006), and return a typed result (committed | rolled-back + errors). Wire
  `cmd/agentd/main.go` to serve this handler bound to **loopback only** (127.0.0.1 or a unix socket) —
  NEVER a public interface (spec §12.1 default-deny inbound).

**Trust boundary — full fail-closed + intrinsic-safe (per AGENTS.md), because /apply takes untrusted
JSON:**
- Decode the body into typed Go structs with a strict decoder (reject unknown fields:
  `Decoder.DisallowUnknownFields`); reject malformed/oversized/exotic bodies with a typed 4xx error,
  never a panic. Bound the request body size.
- The plan's operations may only name registered transactional capabilities and carry typed requests;
  the engine's atomic precondition already rejects unknown ones — surface that as a clean 4xx, apply
  nothing. Request data NEVER becomes a file path or command.
- No `os/exec`/shell anywhere. No auth yet (loopback trust); document that as a known gap.

## User value
This is how a declared plan actually reaches the host: controller → loopback → agent validate → apply
→ rollback-on-failure (spec §8.2, FR-007). Read endpoints give the UniFi-style status (FR-008).

## Non-goals
- No real network/TLS/auth/identity; loopback only. No RPC/protobuf (typed JSON for now).
- No new capabilities; uses the registry + engine + discovery already built.

## Dependencies
P1-004 (status/registry), P1-005 (discovery), P1-006 (transaction engine).

## Acceptance tests
`acceptance_command` (go test in golang:1.26) + `go vet` clean. Via `httptest` (no real socket):
1. `GET /healthz` and `GET /capabilities` return 200 + the expected JSON (read-only; no secrets).
2. `POST /apply` with a valid plan (using a mock TxCapability) returns committed; a plan whose op
   fails returns rolled-back (the engine result surfaced as typed JSON).
3. **Fail-closed body handling:** malformed JSON, unknown fields, an oversized body, and a plan naming
   an unregistered capability each return a typed 4xx, apply NOTHING, and never panic.
4. The handler rejects non-GET on read routes / non-POST on /apply (405), and unknown paths (404).

## Performance & security constraints
TCB. Loopback-only bind; default-deny. Strict JSON decode (DisallowUnknownFields + size limit);
fail-closed; no panic on bad input. No `os/exec`/shell/arbitrary surface. `go vet` clean. Deterministic
httptest. R3 → reviewer-gated, extra scrutiny on the untrusted /apply boundary + loopback binding.

## Hardware profiles
n/a (arch-neutral Go; loopback).

## Allowed tools & network
Build/test only in golang:1.26 via `tools/build/go-in-docker.mjs`. Tests use httptest (no real net).

## Required artifacts
- `agent/transport/server.go`, `agent/transport/server_test.go`, updated `agent/cmd/agentd/main.go`.

## Rollback plan
Revert branch `task/P1-008`.

## Definition of done
- [ ] `go test ./...` + `go vet ./...` clean in the container.
- [ ] /healthz + /capabilities read-only; /apply validates fail-closed + runs the engine (commit/rollback).
- [ ] Malformed/unknown-field/oversized/unknown-capability bodies ⇒ typed 4xx, apply nothing, no panic.
- [ ] Loopback-only bind; no os/exec/shell/arbitrary surface; method/path guards (405/404).
- [ ] Diff stays within `agent/`.
