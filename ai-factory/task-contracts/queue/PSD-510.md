---
id: PSD-510
title: Hydrate first-party apps at launch — kill the baked-HTML placeholder, wire viewmodels to host-bridge ports
status: ready
phase: 6
pod: desktop
risk_class: R2
fr: []
depends_on: []
target_paths:
  - ui_kits/desktop/viewmodels/app-registry.ts
  - ui_kits/desktop/viewmodels/apps
  - ui_kits/desktop/runtime
  - sdk/typescript/test/ui-kits
acceptance_command: "npm run typecheck && node --experimental-strip-types --test sdk/typescript/test/ui-kits/app-hydration.test.ts"
allowed_network: false
budget_minutes: 120
---

## Objective
Remove the **baked-HTML placeholder** behavior of first-party app launch and replace it with REAL,
port-backed hydration. Today the app viewmodels under `ui_kits/desktop/viewmodels/apps/` (created by
`createTerminalAppViewModel`/`createMailAppViewModel`/`createBrowserAppViewModel`/`createEditorAppViewModel`/
`createFilesAppViewModel`/`createActivityMonitorAppViewModel`/`createSettingsAppViewModel`) are **orphaned**:
a grep for those factories outside `viewmodels/apps` and `sdk/typescript/test/ui-kits` returns nothing.
`createAppRegistryViewModel` (`ui_kits/desktop/viewmodels/app-registry.ts:55`) calls
`host.launchApp(app.descriptor)` (line 112) which mounts the static `index.html` and **never instantiates a
viewmodel or runs the binder** — so every first-party app renders its baked sample HTML and the port-backed
viewmodel layer is dead in the running OS. `bootstrap.ts` `DEFAULT_SCREEN_MODULES` (`ui_kits/desktop/runtime/bootstrap.ts:57-66`)
mounts none of the app viewmodels.

Add a **per-app hydration step** at launch:
- After `host.launchApp(...)` returns a launch (with its web/tsx surface), the registry (or a new
  `ui_kits/desktop/runtime/app-hydrate.ts` it delegates to) must, for each first-party app id, instantiate
  the app's viewmodel with its **injected host-bridge ports** and run the existing binder
  (`createBinder` in `ui_kits/desktop/runtime/binder.ts`, which already supports
  `data-vita-bind-text`/`data-vita-bind-list`/`data-vita-action`) over the launched surface's DOM.
- Wire a first-party `AppPackage` registry whose entries carry: the launchable descriptor, the viewmodel
  factory, and the host-bridge ports each viewmodel needs (injected — NO ambient I/O, NO platform internals).
- The hydrated state MUST come from the **injected real ports**, not from any hardcoded sample/baked literal.
  Removing the placeholder means: the bound text/list nodes reflect values pulled through the port, and when
  a port is absent the viewmodel/hydration **fails closed honestly** (returns an error result / leaves the
  surface unhydrated with a signal) rather than silently showing baked content.
- Import the SDK exactly as the existing `viewmodels/apps/*.ts` files do; use ONLY public `@vita/desktop-sdk`
  exports.

## User value
First-party apps (terminal/mail/browser/editor/files/activity-monitor/settings) become **live** in the running
desktop OS: launching one shows real, port-driven content instead of a frozen mockup. Closes the dead-layer gap
where built viewmodels were never instantiated or rendered.

## Non-goals
- NO new visual design / CSS — the app `index.html` files remain the design of record; this wires logic to them.
- NO new SDK ports and NO re-implementation of existing viewmodels — consume them.
- NO CEF/compositor changes; NO platform internals; NO DOM engine — bind against the existing
  `VitaElement`/binder abstraction used by the headless tests.

## Acceptance
`acceptance_command` passes. A new deterministic, headless test
`sdk/typescript/test/ui-kits/app-hydration.test.ts` (run by the EXACT path above — no glob) MUST assert,
against fake `VitaElement` surfaces + fake host-bridge ports:
1. **Placeholder is GONE / real behavior present:** after launching a first-party app, the hydration step
   instantiates that app's viewmodel and the binder writes the value supplied by the **injected fake port**
   into the surface's `data-vita-bind-text`/`data-vita-bind-list` nodes. The test seeds a port-returned value
   that differs from any literal in the app's static HTML and asserts the bound DOM shows the **port value**,
   proving content is no longer the baked sample.
2. **Fail-closed honesty:** when the required host-bridge port is missing/denied, hydration returns an error
   result (or refuses to hydrate with an explicit signal) — it does NOT fall back to baked sample content.
3. **No orphans:** the first-party registry instantiated by launch references every `create*AppViewModel`
   factory listed in the objective (assert each is wired/invoked), so a grep for those factories outside
   `viewmodels/apps`/tests would now find the launch wiring.
4. `npm run typecheck` is clean.

## Security/constraints
- Injected ports only (no ambient I/O); capability-mediated, fail-closed (mirror the
  `hasDesktopCapabilityGrant` posture in `viewmodels/app-host.ts`). Pure/deterministic hydration logic.
- No remote imports; lockfile mandatory; no package lifecycle scripts; no FFI/native addons.
- Do NOT weaken or delete existing app-viewmodel tests; do NOT edit hidden/rotating eval suites.

## Hardware profiles
- Headless Node test (`--experimental-strip-types`); no GPU/CEF/display required. Portable-first.

## Allowed tools + network
- Local toolchain only (`npm run typecheck`, `node --test`). `allowed_network: false`.

## Compute/time budget
- 120 minutes wall; single worktree; no network.

## Required artifacts
- Edited `ui_kits/desktop/viewmodels/app-registry.ts` (and/or new `ui_kits/desktop/runtime/app-hydrate.ts`
  + a first-party app-package registry module under `ui_kits/desktop/`) performing launch-time hydration.
- New `sdk/typescript/test/ui-kits/app-hydration.test.ts`.
- `result.json` referencing PSD-510 with the acceptance transcript.

## Rollback plan
- Single branch `task/PSD-510`; revert the merge commit. Hydration is additive at the launch seam — reverting
  restores the prior (placeholder) launch path with no schema/state migration.

## Definition of done
- Launch hydrates first-party apps from injected real ports; the baked-sample placeholder path is removed;
  absent ports fail closed honestly; `app-hydration.test.ts` + `npm run typecheck` pass via the exact
  acceptance command. R2 — escalate per CLAUDE.md §3 (independent verify + GPT-5.5 xhigh review before merge).
