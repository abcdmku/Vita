---
id: P2-002
title: Permission broker — capability-grant decision model (default-deny, least-privilege)
status: ready
phase: 2
pod: identity-security
risk_class: R2
fr: [FR-011, FR-016]
depends_on: [P1-001]
target_paths:
  - runtime/permission-broker
acceptance_command: "node --experimental-strip-types --test \"runtime/permission-broker/test/*.test.ts\""
allowed_network: false
budget_minutes: 60
artifacts:
  - runtime/permission-broker/src/grants.ts
  - runtime/permission-broker/src/decide.ts
  - runtime/permission-broker/test/decide.test.ts
---

## Objective
Build the **pure decision core** of the permission broker (spec §7.1 policy decision engine, §11
per-app data/network capabilities, §12.2 default-deny network policy). This is R2 — independently
reviewed before merge. Provide:
- Typed `CapabilityRequest` and `CapabilityGrant` models for **data** access (class + mode
  read-only/read-write, scoped) and **network** access (ingress/egress, default-deny), reusing the
  package-contract declarations from P1-001 (`sdk/manifests`) where natural (import, don't duplicate).
- `decideGrants(request: CapabilityRequest, policy: BrokerPolicy): GrantDecision` — a **pure,
  deterministic, default-deny, least-privilege** function: grant only what the policy explicitly
  allows AND the package declared; deny by default; never escalate beyond the destination policy
  (spec §13.1 "import never grants more permissions than the destination policy permits"). Return a
  typed decision listing granted vs denied capabilities + reasons. Fail-closed and **never throw** on
  malformed/`unknown`/cyclic input (per AGENTS.md) — unknown/ambiguous ⇒ deny.

## User value
The broker is a trusted-computing-base component: it is the choke point that keeps untrusted
workloads least-privileged (spec §7.1, §11, §12.2). Its decision logic must be conservative and
auditable.

## Non-goals
- No enforcement/runtime wiring, no Deno integration, no sockets — the **decision function only**.
- No identity/auth; assume a resolved policy is passed in.

## Dependencies
P1-001 (package contract declarations).

## Acceptance tests
`acceptance_command`. `runtime/permission-broker/test/decide.test.ts` must prove:
1. A request for a capability the policy allows AND the package declared is granted; one the policy
   forbids (or the package didn't declare) is denied with a reason.
2. **Default-deny:** an undeclared/unknown capability is denied (not silently allowed).
3. Import/escalation guard: a request exceeding the destination policy is denied (no escalation).
4. Fail-closed: malformed/cyclic/`unknown` input yields all-denied, never throws.

## Performance & security constraints
Pure/deterministic, no I/O. **Default-deny, least-privilege, fail-closed, never-throw.** No new
privileged surface. R2 → reviewer-gated.

## Hardware profiles
n/a.

## Allowed tools & network
Offline. Node built-ins + in-repo SDK/manifests only. No new deps.

## Required artifacts
- `runtime/permission-broker/src/grants.ts`, `src/decide.ts`, `test/decide.test.ts`.

## Reviewer revision (round 2 — BLOCKING, from P2-002.review.md)
Round 1 was fail-closed only for *wholly* malformed input; **partial-but-usable** malformed inputs
could still grant. This is a TCB component — fix all four:
1. **Strict data-declaration validation.** Reject (→ deny) any data requirement/volume that is not a
   complete, well-formed shape: require `classes`; require the full `DataVolumeRequirement` fields the
   package contract declares (e.g. `name`, `class`, `access`, `mountPath`, `persistence`, `backup`,
   `sizeMiB`). A `DataRequirements`-typed value missing required fields must NOT authorize a grant.
   **Reuse `validatePackageContract`/the P1 manifest shape validation** rather than a looser local
   re-check (also addresses the comprehension note below).
2. **Network policy must be complete.** `NetworkPolicy` requires BOTH `ingress` and `egress`. A
   policy missing either side is malformed → reject and all-deny (do NOT grant the present side). A
   `{ network: { egress: [...] } }` destination policy must yield all-denied.
3. **No undocumented aliases.** Read package declarations ONLY from `request.packageContract` (the
   typed field). Remove the `request.package` alias entirely; if `packageContract` is absent or
   unreadable → deny (unknown/ambiguous ⇒ deny). Do not widen the trust-boundary API beyond the type.
4. **Adversarial tests** for the above: a request with a partial-but-typed data declaration (missing
   required volume fields) → denied; a destination policy with only `egress` → egress denied; a
   request relying on a `package` alias / absent `packageContract` → denied. Keep the existing tests.

Comprehension note (rubric, TCB): the decision core is too large/complex to audit — tighten it by
delegating shape validation to the P1 manifest validator and keeping `decideGrants` to the grant
rules only. Smaller + auditable is required for this component.

## Reviewer revision (round 3 — BLOCKING, from P2-002.round2 review)
Round 2 still executed **attacker-supplied methods** while deciding: a shape-valid contract whose
array has a shadowed method (e.g. `contract.network.egress.some = () => true`, or shadowed
`includes`/`find`, or a hostile `Symbol.iterator`) made an undeclared capability appear declared.
Re-architect for trust-boundary safety and auditability:

1. **Normalize-then-decide.** Phase A: convert ALL untrusted input (request + policy + package
   declarations) into **plain, trusted internal data** using ONLY intrinsic-safe reads — `Array.isArray`,
   index access (`arr[i]`), `Object.hasOwn`, `Reflect.ownKeys`, and `Array.prototype.X.call(arr, …)`
   on already-validated plain arrays. **Never call a method off an untrusted object** (`x.includes`,
   `x.some`, `x.find`, `x.forEach`, `for…of` on untrusted iterables). Reject (→ all-deny) any exotic
   shape: non-plain arrays/objects, method-shadowed arrays, hostile iterators, getters, proxies,
   cycles, partial/missing-required fields. Phase B: the grant-decision rules operate ONLY on the
   plain normalized data.
2. **Keep it small and auditable.** This is a TCB path — target a compact decision core (aim well
   under ~400 lines total). Delegate package-shape validation to `validatePackageContract` (P1-001)
   where possible; do not hand-roll a large parallel validator.
3. **Regression tests** proving all-denied (never grants undeclared capability) for: a package/policy
   with shadowed array methods (`some`/`includes`/`find` overridden to lie); a hostile
   `Symbol.iterator`; a getter/proxy that returns different values per read. Keep all existing tests.

## Rollback plan
Revert branch `task/P2-002`.

## Definition of done
- [ ] Acceptance passes cleanly.
- [ ] Default-deny + least-privilege + no-escalation proven; fail-closed never-throws (incl. cyclic).
- [ ] Imports package-contract types (no duplication); diff within `runtime/permission-broker`.
