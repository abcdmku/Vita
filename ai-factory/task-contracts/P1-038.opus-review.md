# Opus adversarial cross-review — P1-038 (On-device READ path, VITA-STATE)

**Reviewer:** Opus 4.8 (adversarial cross-reviewer)
**Branch:** `task/P1-038` (diff `main...task/P1-038`)
**Risk class:** R1 (read-only; crosses unprivileged→privileged boundary)
**Date:** 2026-06-23

## VERDICT: approve

All five review axes pass. The slice delivers a pure-read traversal of the unprivileged→privileged
boundary with no new authority, faithful vendoring, correct fail-closed markers, and a thorough test
that I ran green (4/4). No blocking findings.

---

## 1. Read-only + no new privilege — PASS

- **Transport is hard read-only.** `unix-socket-transport.ts:30-33` rejects any non-GET at the
  transport layer (`if (init.method !== "GET") throw new Error("on-device agent transport is
  read-only")`). The vendored client's only mutating method (`apply`, POST) can therefore never
  succeed through this transport even if called. `main.ts`/`agent-state.ts` only ever call
  `getHealth` (`/healthz`), `getOperations` (`/operations`), and `getState` (`/read/{cap}`) — all GET.
- **AF_UNIX-only kept.** `unix-socket-transport.ts:54` uses `Deno.connect({transport:"unix",
  path: socketPath})` scoped to `/run/vita-agent/agentd.sock` (`main.ts:33`). No `Deno.connect` over
  TCP, no AF_INET, no `--allow-net`. No host-FS access beyond the socket.
- **No agentd mutation route called**, and **no Go change** was needed: the diff is confined to `os/`
  (verified `git diff --name-status` — zero `agent/` files). agentd already serves the read routes:
  `server.go:345` `/operations` and `server.go:356-357` `/read/{cap}`, both GET-only
  (`handleOperations` rejects non-GET at `server.go:402-403`; `handleRead` at `server.go:413-414`;
  `readCapability` invokes `capability.Handle` with a *read* request — no mutation, no audit write).
- Response size is bounded (`DEFAULT_MAX_RESPONSE_BYTES = 64KiB`, `unix-socket-transport.ts:19,109`),
  a sensible DoS guard on untrusted agentd output.

## 2. Vendoring fidelity — PASS

- `vita/agent-client.ts` is **1111 lines = upstream 1110 + 1 header line.** Full `diff` against
  `controller/agent-client/src/agent-client.ts` shows ONLY: the `// Vendored from …` header and three
  import-path rewrites (`../../../sdk/typescript/src/{safe-normalize,plan}.ts` → `./{safe-normalize,
  plan}.ts`). No logic fork, no bloat.
- `vita/plan.ts` (+11, **new file** — note: the prompt said "+11 additions" but it is a new file, not
  an edit) is a faithful type-only extraction of `JsonPrimitive`/`CanonicalJsonValue`/
  `CanonicalJsonObject`, byte-equivalent to `sdk/typescript/src/plan.ts:5,59-65`. No logic.
- `safe-normalize.ts` was already vendored into the overlay by S1; reused unchanged.

## 3. Marker correctness + fail-closed — PASS

- Success marker `VITA-STATE: caps=N hostname=x status=OK` emitted at `main.ts:244` via
  `formatAgentStateMarker` (`agent-state.ts:47-49`), built from REAL agentd data: `caps` =
  `getOperations().length` (registered operation names, `server.go:407` / `registry.go:105`),
  `hostname` = `getState("hostname.set").current`.
- Fail-closed marker `VITA-STATE-ERROR: status=FAILSAFE` emitted on (a) connect/health failure
  (`main.ts:226-228`, alongside `VITA-CONNECT-ERROR`) and (b) state-read failure (`main.ts:246`).
- **Never throws out of the oneshot.** `readAgentStateSummary` wraps its entire body in try/catch and
  returns a discriminated `{ok:false, reason}` (`agent-state.ts:23-45`) — it cannot throw. The health
  read is inside its own try/catch. The top-level `main().catch` is a final backstop
  (`VITA-MAIN-ERROR: status=FAILSAFE`, exit 1).
- **Untrusted agentd JSON parsed safely:** the vendored client funnels every response through
  `safeNormalize` (`agent-client.ts:286/432`) before validation; `validateOperations` rejects
  non-string-array entries; and `readHostname` (`agent-state.ts:50-66`) defensively requires `current`
  to be a non-empty string (since `validateCapabilityState` passes the object through as-is). Malformed
  hostname → fail-closed, not a bogus OK marker.
- **All prior markers kept:** VITA-TS, VITA-EVAL, VITA-EVAL-REJECT, VITA-PREVIEW, VITA-PREVIEW-NOOP,
  VITA-PREVIEW-ERROR, VITA-EXPLAIN, VITA-CONNECT, VITA-CONNECT-ERROR (verified in branch `main.ts`).

## 4. Test quality — PASS (ran green)

`node --experimental-strip-types --test os/x86_64/ts-overlay/usr/lib/vita/ts/agent-client.test.ts`
→ **4 pass / 0 fail.** Tests:
1. Builds the right read requests (asserts exact URL/method/headers `/operations` + `/read/hostname.set`,
   GET, `Accept: application/json`, no body) and parses into `caps=3 hostname=vita-node-7`.
2. **Upstream parity** — drives BOTH the vendored and the real `controller/agent-client` against the
   same mock and asserts identical results + identical recorded requests.
3. **Fail-closed** — transport error → `{ok:false, reason:/Agent transport request failed/}`;
   malformed `operations:[42]` → `{ok:false, reason:/expected wire shape/}`. No throw escapes.
4. **Byte-identity guard** — reads both files, strips the header, rewrites imports, asserts the
   vendored body equals upstream. This is a durable anti-drift assertion (a strong addition).

Mock transport is request-build + response-parse only; no weakened/oracle-leaking assertions.

## 5. Diff confined to `os/`; no weakened tests — PASS

Diff touches only the 6 `os/` files (2 modified/new test+main, 4 new `vita/` modules). No `agent/`
change (agentd already had the routes). No existing test was modified or weakened.

---

## NOTES (non-blocking)

- **`caps` semantics:** the marker's `caps=N` is the count of registered *operations*
  (`getOperations` → `/operations`), not a separate "capabilities" enumeration. This is internally
  consistent and matches the test (`caps=3`), but a future reader could conflate "caps" with the
  `/capabilities` route. Cosmetic only; the contract explicitly allows "fields per what agentd
  reports."
- **`getState("hostname.set")` reads only the `current` field.** If agentd ever returns hostname under
  a different key the marker fails closed (STATE-ERROR), which is the safe outcome.
- Orchestrator still owes the on-device host-verify on Borg51 (DoD item 4): confirm `VITA-STATE: …
  status=OK` appears on the serial after `VITA-CONNECT`, both plain and verity. Code review alone
  cannot discharge that gate — but nothing in the change blocks it.
