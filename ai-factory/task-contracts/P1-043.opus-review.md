# Opus adversarial cross-review — P1-043 (S7: on-device capsule PREVIEW)

**Branch:** `task/P1-043` (b0f3195) · **Risk:** R2 (read + pure diff) · **Reviewer:** Opus 4.8 (adversarial)

## VERDICT: approve

Independently verified read-only, faithful vendoring, correct diff semantics, fail-closed, and 6/6
tests passing. No blocking findings. Diff confined to `os/` (3 files, +1125/-13); no test weakening.

---

## Evidence by check

### 1. Read-only — no mutation, no new surface  ✅
- `capsule-preview.ts` calls **only** `client.getState(capability)` for the current registry.
  `agent-client.ts:328-331` maps `getState` → `GET /read/${capability}`, i.e. the read-only route
  (`agent/transport/server.go:356`, `capsule.ReadRequest`/`ReadResponse` at `server.go:307`).
- The new module contains **zero** `Deno.*` / `fetch` / network / filesystem / `/apply` / POST /
  `child_process` calls (grep clean). It reuses the existing AF_UNIX transport
  (`createDenoUnixSocketAgentTransport`) only inside the unused-by-main `…FromAgentd` convenience
  helper; `main.ts:373` passes the already-built read `client` instead.
- `main.ts` types the call as `Pick<AgentClient, "getState">`
  (`capsule-preview.ts:138`, `main.ts:emitCapsulePreviewMarker`) — type-level proof preview cannot
  reach `apply`. Preview uses the read client; apply uses a *separate*
  `createDenoUnixSocketApplyAgentTransport` (`main.ts:309-311`). No new privilege/transport surface.

### 2. Module fidelity (703 lines)  ✅ faithful adaptation, no logic fork
- `previewCapsuleChange`, `diffCapsuleRegistries`, `compareNaturalStrings`, `compareDigitRuns`,
  `appendValidationRejections`, `versionChangeKind`, etc. are line-for-line equivalent to
  `controller/capsule/src/capsule-preview.ts` (only the import path is adapted: upstream imports the
  SDK model via `../../../sdk/...`, vendored imports `./capsule-registry-model.ts`).
- **One deliberate divergence, output-equivalent:** vendored diff stores buckets in
  `Object.create(null)` records via `defineCapsuleRecordValue` (`capsule-preview.ts` diff section)
  instead of upstream plain `{}`. This is **hardening** against `__proto__`/`constructor` capsule-id
  pollution, not a semantics change — confirmed `getPrototypeOf(diff.installed) === null` and the
  serialized JSON is byte-identical to upstream.
- Added (not forked) on top of upstream: the agentd read helpers + marker, modeled on the merged
  `pds-read.ts` pattern (same `safeNormalize` → `expectFields` → `validateCapsuleRegistry` shape).
- Vendored `capsule-registry-model.ts` is byte-identical to `sdk/typescript/src/...` except the
  vendoring-header comment.
- **Parity test is real:** `previewVendoredCapsuleChange` vs `previewUpstreamCapsuleChange` on a
  multi-bucket input, compared via `toPlainJson` deep-equal. I independently reproduced `parity
  equal: true` and confirmed the comparison would catch a real classification divergence.

### 3. Diff correctness  ✅ (independently probed beyond the test)
- Test 1: known install/remove/upgrade → `installed=1 removed=1 upgraded=1` (exact ids asserted).
- Test 2: current==desired → all-zero.
- Test 3: `exists:false` → empty-registry baseline → 1 install. Handled.
- My adversarial probes (valid manifest-passing entries):
  - `2.0.0→1.0.0` + state change → lands in `downgraded` + `stateChanged` (NOT `upgraded`); marker
    correctly reports `upgraded=0` (marker only counts install/remove/upgrade per spec).
  - `1.9.0→1.10.0` → **upgraded** (natural-numeric compare, not lexical — lexical would mis-rank).
  - integrity-only change → `integrityChanged=true`, all count buckets empty.
- Untrusted parse via `safeNormalize`: reads through `getOwnPropertyDescriptor` + data-descriptor
  check (`safe-normalize.ts:321-378`), so accessors are rejected un-invoked and cycles/oversize are
  budget-rejected. Test 5 asserts `getterReads === 0` and cyclic rejection — backed by real defense.

### 4. Marker + fail-closed  ✅
- `formatCapsuleRegistryPreviewMarker` emits exactly
  `VITA-CAPSULE-PREVIEW: installed=<i> removed=<r> upgraded=<u> status=OK` on success and
  `VITA-CAPSULE-PREVIEW-ERROR: status=FAILSAFE` on read/parse failure. Test regex round-trips it.
- `readCapsuleRegistryPreview` is wrapped in try/catch → never throws out of the oneshot.
- **Sequencing (main.ts):** preview emits BEFORE apply; apply is **gated** on preview success
  (`if (await emitCapsulePreviewMarker(client)) { emitCapsuleMarkers } else { CAPSULE FAILSAFE }`) —
  stronger fail-closed than the contract required.
- All prior markers retained (STATE/APPLY/PDS). Both failure branches (connect-fail and state-fail)
  emit `VITA-CAPSULE-PREVIEW-ERROR` + `VITA-CAPSULE-ERROR` FAILSAFE.

### 5. Test quality  ✅ 6/6 PASS
`node --experimental-strip-types --test …/capsule-preview.test.ts` → `# pass 6 # fail 0`.
Full overlay suite re-run for regression (the `TEST_CAPSULE_ENTRY → ON_DEVICE_CAPSULE_ENTRY`
rename): agent-client 4/4, apply 6/6, capsule 6/6, eval 4/4, explain 5/5, pds-read 4/4, preview
13/13, safe-normalize-parity 2/2 — all green.

### 6. Scope / no weakened tests  ✅
- All 3 changed paths under `os/`. Only test file changed is the new `capsule-preview.test.ts`
  (purely additive); no existing `.test.ts`/`_test.go` weakened or removed.

---

## NOTES (non-blocking)
- `readCapsuleRegistryPreviewFromAgentd` is exported but unused by `main.ts` (which injects the
  client). Harmless parallel to `pds-read.ts`'s `…FromAgentd`; keeps the read self-contained for
  future callers. No action needed.
- Marker intentionally surfaces only install/remove/upgrade counts; downgrade / stateChange /
  integrityChange are computed in the diff but not in the marker (matches the spec's marker grammar).
- Orchestrator still owes the host-verify (Borg51): confirm `VITA-CAPSULE-PREVIEW: …=.. status=OK`
  appears on the serial *before* `VITA-CAPSULE` (DoD item 3). Code path is correct; boot confirmation
  is the remaining gate.
