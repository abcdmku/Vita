
## REVISION 5 (Borg51 boot: VITA-CAPSULE-LIFECYCLE-ERROR reason=update_capsule_lifecycle_backup_failed — 2026-06-24)
rev4 logic is R2-approved + go-verified, MERGED to main — but on a REAL boot the lifecycle UPDATE fails at the
backup-before-update step. Diagnosis: the backup TARGET (/var/lib/vita-backups) is fine (the P1-066 backup slice
VITA-BACKUP writes there successfully on-device). The failure is the SOURCE: the on-device lifecycle proof's test capsule
lacks a real backable volume/state dir, so archive.Create reads source roots that don't exist (unit tests used fakes).
FIX: runCapsuleLifecycleProof (os/.../ts/vita/capsule-lifecycle.ts) must set up its test capsule WITH an actual backable
volume/state dir on-device BEFORE the update — mirror how the working VITA-BACKUP proof (backup.ts) establishes its source
— so backup-before-update (Create+Verify) succeeds and the proof emits VITA-CAPSULE-LIFECYCLE: update=OK
rollback-on-unhealthy=OK. (Lifecycle is already merged; this fixes the on-device proof setup.) Verify: Borg51 boot.
