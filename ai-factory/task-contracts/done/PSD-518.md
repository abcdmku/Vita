---
id: PSD-518
title: agentd /healthz Healthy must be computed from real subsystem state (remove canned true)
status: ready
phase: 6
pod: desktop
risk_class: R3
fr: []
depends_on: []
target_paths:
  - agent/status/status.go
  - agent/status/status_test.go
acceptance_command: "cd agent && go test ./status/"
allowed_network: false
budget_minutes: 90
---

## Objective
Remove the canned `Healthy: true` placeholder in agentd's `/healthz` handler and replace it with a
**real, measured aggregate health signal** folded from live subsystem state.

Evidence: `agent/status/status.go:61-77` — `(*handler).status()` builds `Status{ ..., Healthy: true }`
**unconditionally**. The `Healthy bool` field (`agent/status/status.go:15`) is documented/intended as an
aggregate health signal but is never computed from any subsystem; capabilities and uptime are real, only
the boolean is hardcoded. A node with a down capsule, an unready transport, or degraded storage still
reports `"healthy": true`, which is a false readiness signal that can mask an unhealthy node from
operators and orchestration.

Required change:
- Add an injected **health source** (an interface/func field on `handler`, wired via `newHandler`/`NewHandler`)
  that returns the measured aggregate health. Compute `Healthy` by folding the *worst-of* across the real
  inputs that are available: capsule `Supervisor.Snapshot()` worst-of workload `Health`
  (`agent/internal/capsule-runtime/health.go:249` — `StatusDown`/`StatusUnhealthy`/`StatusUnknown` ⇒ not healthy),
  transport/registry readiness, and `storagehealth` (`agent/internal/storagehealth/storagehealth.go` —
  `StatusDegraded`/`StatusUnknown` ⇒ not healthy). The fold MUST be fail-closed: any unhealthy/degraded/unknown/unready
  input ⇒ `Healthy: false`; `Healthy: true` ONLY when every consulted subsystem reports healthy.
- Inject the health source so it is unit-testable with fakes (no ambient I/O, no real systemctl/sysfs in tests),
  exactly mirroring how `now clock` is already injected through `newHandler`.
- `NewHandler` keeps a usable production default. Do NOT widen the JSON `/healthz` body to leak secrets
  (the existing `secret`/`token`/`password` guard in the test must keep passing). Keep capabilities/uptime
  behavior unchanged; ordering/determinism preserved.

If, and only if, the team concludes `/healthz` must remain strict process-liveness, the alternative is to
rename/document the field as `liveness` and add a separate `readiness` (or `aggregateHealth`) field that
carries the measured fold above — the canned boolean must not survive under the name `Healthy` either way.
Default to computing `Healthy` from real state.

## User value
Operators and on-device control plane get a truthful node-health signal: a node with a down capsule,
unready transport, or degraded storage reports `"healthy": false` instead of a canned `true`, so failing
nodes are visible instead of silently masked.

## Non-goals
- No new HTTP routes/methods beyond the existing `/healthz` GET. No network calls (probes are injected/faked in tests).
- Do not re-implement the capsule supervisor, storagehealth, or transport — consume their existing snapshot/report types.
- No change to capabilities-list or uptime semantics. No secret/PII added to the response body.

## Acceptance
`acceptance_command` passes: `cd agent && go test ./status/`.

The test suite (`agent/status/status_test.go`) MUST, against injected/fake health inputs, assert ALL of:
1. **Placeholder is gone (negative):** with a fake health source reporting an unhealthy/degraded/unknown
   subsystem (e.g. a `StatusDown` capsule workload, or `StatusDegraded` storage), `/healthz` returns
   `"healthy": false`. A grep/AST check that no code path sets `Healthy` to a literal `true` independent of
   the injected source (the boolean must come from the injected health source, not a constant).
2. **Real healthy path (positive):** with a fake source reporting every consulted subsystem healthy,
   `/healthz` returns `"healthy": true`.
3. **Fail-closed worst-of:** with multiple inputs where at least one is not-healthy, the aggregate is
   `false` (worst-of dominates); when the source has no information (empty/unknown), it does NOT silently
   report healthy.
4. Existing assertions still hold: 200 + `application/json`, real version/uptime/sorted capabilities, and
   the `secret`/`token`/`password` body guard.

Tests must be deterministic and hermetic (injected clock + injected health source; no real systemctl, sysfs,
or sockets).

## Security/constraints
- Fail-closed aggregation: unknown/unready/degraded ⇒ not healthy. No secrets/PII in the `/healthz` body.
- Injected dependencies only in tests (no ambient I/O); deterministic. R3 — privileged agent boot/identity-adjacent
  surface: no behavior change to capability gating; response stays minimal.

## Hardware profiles
- Borg51 QEMU host (linux/amd64). Tests are pure Go unit tests; no hardware required.

## Allowed tools + network
- Tools: go toolchain, editor. Network: **false** (no remote imports; module cache only; lockfile/go.sum mandatory).

## Compute/time budget
- 90 minutes wall; single worker; `go test ./status/` well under 1 minute.

## Required artifacts
- Modified `agent/status/status.go` (injected health source; `Healthy` computed from it; no literal-true path).
- Modified `agent/status/status_test.go` covering negative (placeholder-gone), positive, fail-closed worst-of, and the existing guards.

## Rollback plan
- Revert the `task/PSD-518` branch merge; the change is confined to `agent/status/` and reintroduces no
  external dependency, so reverting restores prior behavior with no migration.

## Definition of done
- `Healthy` in `/healthz` is computed from an injected real-subsystem health fold; the canned `Healthy: true`
  is removed; `cd agent && go test ./status/` passes with the negative + positive + fail-closed assertions
  above; typecheck/build clean. R3 — escalate per CLAUDE.md §3 reviewer gate (`npm run review -- PSD-518`).

## Reviewer revision (round 1)

Codex R3 review: REVISE. The /healthz fix only wires STORAGE health; it must aggregate ALL subsystems (worst-of fold). Fix BOTH BLOCKING:
1. defaultHealthSource (agent/status/status.go ~:138) only consults storagehealth.Collect, so aggregateHealthy can return true while capsule or transport state is unknown/unready. Wire the LIVE capsule Supervisor.Snapshot() + transport/registry readiness into the production health source, and fold WORST-OF across capsule, transport/registry, AND storage.
2. NewHandler (status.go ~:42) has no production path to receive the live capsule supervisor/registry/transport state - it always uses the storage-only default. Give it a production wiring path so the real /healthz signals the REAL aggregate (not storage-only). Add a test asserting the production source reflects an UNREADY capsule/transport (degrades health), not just injected fakes.


## Reviewer revision (round 2)

Codex R3 review: STILL fail-open. The aggregate MUST fail CLOSED. Fix ALL:
1. NewHandler (agent/status/status.go ~:42,69-74) must NOT permit a zero/partial HealthConfig to be storage-only-OK. A HealthConfig missing capsule/transport/registry readiness MUST make /healthz UNHEALTHY (fail-closed), not healthy. defaultHealthSource(HealthConfig{}) returning storage-only is the bug.
2. Wire the REAL sources at the PRODUCTION CALL SITE: find where agentd constructs the status handler and pass the live capsule Supervisor.Snapshot() + transport/registry readiness in. The production /healthz must reflect the REAL aggregate.
3. Registry/transport readiness must be MEASURED, not synthesized 'ready := true' for any non-nil registry (status.go ~:208-219). Unknown/unwired readiness => UNHEALTHY (fail-closed), never silently OK.
4. aggregateHealthy must fold WORST-OF across capsule, transport/registry, AND storage; if any required input is absent/unknown => unhealthy.
5. Strengthen the AST guard to also reject hardcoded readiness literals (ready := true) feeding the aggregate, not just Healthy: true. Add a test: zero HealthConfig => unhealthy; unwired transport => unhealthy.



## REVISION ROUND (orchestrator 2026-06-26) � address ALL blocking findings below, then re-run the acceptance command
BLOCKING: NewHandler defaults to an empty variadic HealthConfig, so /healthz can be constructed with NO measured sources � production wiring is still optional. FIX: the PRODUCTION agentd serving path must construct the health handler with REQUIRED live sources (capsule liveness, transport/registry readiness, storage health) � not optional/empty. Add a test that the SERVED production /healthz path computes Healthy from real subsystem state and contains no canned Healthy:true (assert against the production wiring, not just the helper package file).
Full reviewer notes: ai-factory/task-contracts/PSD-518.review.md



## REVISION ROUND (orchestrator 2026-06-26) � readiness signals are still tautological/static
BLOCKING: server.go:604-608 wires /healthz transport readiness to requestContextReadinessSource = ctx.Err()==nil (server.go:1191-1193) � ALWAYS true for any served request, so an unready transport/listener is never folded into Healthy. Registry readiness (1165-1188) is a STATIC self-check on construction-time names, not dynamic. FIX: fold a REAL measured transport readiness (a live transport-up/accepting flag the serving loop SETS when the listener is accepting and CLEARS on shutdown/error) and a DYNAMIC registry readiness (actual current registration state), worst-of + fail-closed, in the SERVED production /healthz path. If no genuine readiness signal exists yet, ADD a minimal real one � do NOT use ctx.Err() or construction-time snapshots.

