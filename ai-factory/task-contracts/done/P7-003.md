---
id: P7-003
title: Agent capability wiring round 6 — register timesync in agentd
status: done
phase: 7
pod: go-agent
risk_class: R3
fr: [FR-004, FR-007]
depends_on: [P6-006, P7-002]
target_paths:
  - agent
acceptance_command: "node tools/build/go-in-docker.mjs --dir agent test ./..."
allowed_network: false
budget_minutes: 40
artifacts:
  - agent/cmd/agentd/main.go
  - agent/transport/server.go
  - agent/transport/server_test.go
---

## Objective
Register the `timesync` capability (P7-002) into `agentd`, exactly like P6-006 wired capsule. Construct it
with its production facade in `agent/cmd/agentd/main.go` and add it to the registry; add its request
decoder to `DefaultRequestDecoders()` in `agent/transport/server.go`. After this, all ELEVEN capabilities
are discoverable via `GET /operations`, applicable via `POST /apply`, and readable via `GET /read/{capability}`.

## User value
Completes the agent's exposed operation set — the NTP time-sync config becomes a managed + readable
operation (FR-004/007).

## Non-goals
- No new capabilities, no transport behavior changes beyond registration + decoder wiring. No real NTP.

## Dependencies
P6-006 (wiring pattern + the existing 10-cap registration), P7-002 (timesync). Both merged.

## Acceptance tests
`acceptance_command` (go test in golang:1.26) + orchestrator `go vet`. Extend the transport integration
test: `GET /operations` returns all ELEVEN registered names (sorted); `POST /apply` routes a plan to a
timesync op and commits (injected facade); `GET /read/timesync` returns the config. timesync must have a
registered decoder.

## Performance & security constraints
TCB. Loopback-only + fail-closed /apply unchanged. Production facade wired. Every registered capability
MUST have a decoder. `go vet` clean. R3 → reviewer-gated.

## Hardware profiles
n/a.

## Allowed tools & network
golang:1.26 via `tools/build/go-in-docker.mjs`. Offline. httptest.

## Required artifacts
- updated `agent/cmd/agentd/main.go` (+timesync), `agent/transport/server.go` (+1 decoder),
  `agent/transport/server_test.go` (integration covers all 11).

## Rollback plan
Revert branch `task/P7-003`.

## Definition of done
- [ ] `go test ./...` + `go vet ./...` clean in the container.
- [ ] /operations lists all 11 caps (sorted); /apply routes to timesync; /read/timesync returns the config.
- [ ] timesync has a registered decoder. Diff within `agent/`.
