BLOCKING

- `absolutePath` is stricter than the cited agent storage validator, so manifest≡agent does not hold. The diff rejects paths longer than 4096 and any non-printable/non-ASCII byte at `agent/internal/capmanifest/capmanifest.go:1518` and `:1524`, mirrored in TS at `sdk/typescript/src/capability-manifest.ts:1956` and `:1964`. The current `agent/capabilities/storage.isCanonicalAbsolutePath` rejects root, trailing slash, empty/`.`/`..` segments, and NUL, but it does not enforce those length/printable-ASCII bounds. Either the agent rule must already enforce this, or the manifest format must not add stricter behavior under this contract.

NOTES

- The `/` corpus flip is correct for the current contract and current storage validator.
- The `systemdUnitName`, `posixUsername`, and `groupName` structured checks appear aligned with the cited agent behavior from this diff.

VERDICT: revise