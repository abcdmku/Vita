**BLOCKING**
- `hostnameRFC1123` is checked after Unicode lowercasing, so the raw value is not guaranteed to contain only `[A-Za-z0-9.-]` as required. In `agent/internal/capmanifest/capmanifest.go`, `normalized = strings.ToLower(raw)` runs before `validateStringFormat(schema.Format, normalized)`. In `sdk/typescript/src/capability-manifest.ts`, `const normalized = schema.lowercase ? value.toLowerCase() : value;` is likewise used for `validateStringFormat(...)`. Values such as `"\u212A.example"` can normalize to `k.example` and pass even though the original hostname contains a non-ASCII character. The format validator must reject the original raw hostname characters and only use lowercase for canonicalization/dedup.

**NOTES**
- The Go acceptance lane passed per the provided orchestrator output. I did not find target-path scope creep in the listed diff.

VERDICT: revise