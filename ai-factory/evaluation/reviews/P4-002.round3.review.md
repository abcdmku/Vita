**BLOCKING**

1. `packages/catalog/src/lockfile-policy.ts:64` / `validateNode`: JSR/Deno-style dependency declarations under `specifiers` are ignored. Because only `DEPENDENCY_CONTAINER_FIELDS` are enforced, a lockfile can include `specifiers: { "jsr:@scope/pkg": "^1.0.0" }` with no resolved entry/integrity and still pass if another marker is present. That violates “every dependency at ANY depth” and the JSR side of the contract. Add canonical handling/tests for JSR specifier maps.

2. `packages/catalog/src/lockfile-policy.ts:145` / `isRemoteOrUrlSpecifier`: remote/URL detection is too narrow. Source fields such as `resolved: "ipfs://..."`, `sourceUrl: "data:..."`, or protocol-relative `//cdn.example/pkg.tgz` are accepted with a valid version/integrity, despite the contract forbidding remote/URL imports. Reject generic URL schemes/protocol-relative URLs unless there is a deliberately allowed local mirror scheme, and add regressions.

**NOTES**

The diff stays within target paths and uses `safeNormalize` before inspection. The supplied acceptance passed and the tests cover the round-2/round-3 regressions, but the gaps above are policy bypasses rather than test-integrity issues.

VERDICT: revise