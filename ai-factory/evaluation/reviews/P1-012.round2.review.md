**BLOCKING**
- `os/x86_64/uki.mjs:102-157` still declares the executable UKI build against `/inputs/vmlinuz`, `/inputs/initrd.img`, and `/inputs/linuxx64.efi.stub`, but no committed artifact or producer step actually materializes those files. `producedInputs` is only metadata, and `os/x86_64/test/uki-determinism.test.ts:387-390` treats self-declaration in `plan.producedInputs` as proof of backing. The command can still be non-consumable while acceptance passes. Add a real declared materialization/precondition model with test coverage, or mark assembly non-executable until those external digest refs are materialized.

- `os/x86_64/uki.mjs:480-498` validates the `test:` key/cert IDs but not `TestKeyPathEnv` / `TestCertPathEnv`, then emits those env names as runtime key/cert references at `os/x86_64/uki.mjs:289-303`. A config can keep the approved `test:` IDs but point the runtime path env at a production/release signing variable. That violates the “only TEST key referenced” requirement. Lock these to the committed TEST env names, or enforce a strict TEST-only env-name policy and add rejection tests.

**NOTES**
- The diff stays within `os/` and does not modify protected paths or existing tests.
- The committed `uki.os-release` addresses the prior os-release dangling-path blocker.
- The signing step is no longer modeled as an executable fake `ukify sign` command with abstract `test:` args, which is the right direction.

VERDICT: revise