BLOCKING

- `agent/capabilities/hostname/set_linux.go:86-91`: Linux `Set` mutates the kernel hostname before the `/etc/hostname` rename can fail. If `syscall.Sethostname` succeeds and `os.Rename` fails, `Apply` returns an error before `transaction.Apply` records an undo, leaving the live hostname changed with no rollback path. This violates the transactional capability contract and the P1-007 “single commit point” lesson.

- `agent/capabilities/hostname/set_linux.go:11,86`: the contract explicitly requires `golang.org/x/sys/unix` for the `sethostname` syscall. This uses the standard `syscall` package instead. For an R3 privileged change, that contract mismatch should be fixed.

- `agent/capabilities/hostname/hostname_test.go`: tests do not cover the dangerous partial-failure case above. The in-memory facade only proves success and later-operation rollback, not that a failed Linux apply cannot leave an untracked hostname mutation. Add a regression around the commit-point/failure semantics after restructuring the facade.

NOTES

- Scope is otherwise contained to `agent/capabilities/hostname/` and does not modify `cmd/agentd/main.go`.
- DNS-label validation is strict for the listed cases and rejects uppercase/control/FQDN-style input.
- The non-Linux stub implementation looks simple, but the current stub test is skipped in the Linux acceptance lane, so it is not proven by the provided command.

VERDICT: revise