**BLOCKING**

- `agent/capabilities/storage/storage.go:48` and `agent/capabilities/storage/storage.go:361`: `quotaGiB` is modeled as a non-pointer `int64` with `omitempty`, and validation allows `0`. That means an explicit transport payload with `"quotaGiB":0` is accepted and normalized away as if quota were absent. P1-018 requires mirroring the SDK storage model’s fail-closed invariants; quota is optional, but when present it must be a valid positive quota. Preserve presence, reject explicit zero, and add a regression test.

**NOTES**

- Scope is otherwise tight: only `agent/capabilities/storage/*` changed.
- No `os/exec` or real Btrfs operations are introduced.
- Transaction commit/rollback behavior follows the existing nodeconfig-style pattern.

VERDICT: revise