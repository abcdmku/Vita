**BLOCKING**

- `agent/internal/capmanifest/nodeconfig_conformance_test.go:91-97` does not drive the actual agent `node.config` request decode/validate entrypoint. It unmarshals the corpus directly into `nodeconfig.Config` and calls `Config.Validate()`, bypassing `transport.DecodeJSONRequest[nodeconfig.ApplyRequest]` and `ApplyRequest.UnmarshalJSON`. The real agent `/apply` path decodes `nodeconfig.ApplyRequest` with a `desired` envelope, so this test can pass while manifest and agent request handling disagree. That violates the contract requirement to prove manifest parity against the agent’s actual validator/decode entrypoint.

**NOTES**

- `runtime/evaluator/test/evaluate.test.ts:78-91` now expects a plan request shaped as `{ mode, remoteAccess }`. Current transport tests still show the agent `/apply` request for `node.config` as `{ desired: { mode, remoteAccess } }`. This may be an intentional manifest-layer shape, but it is worth resolving explicitly because the product spec frames evaluator output as a plan applied through the Go agent.

VERDICT: revise