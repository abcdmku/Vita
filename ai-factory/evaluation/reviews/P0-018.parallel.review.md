**BLOCKING**

- `packages/catalog/src/catalog-entry.ts`: the new `safeNormalize` failure path collapses every normalization error to `path: ""` via `addError(errors, [], normalized.reason)`. That is a behavior change, not just a refactor. The pre-existing tests required nested fail-closed paths such as `sbom/self`, `signatures/some`, `signatures/Symbol(Symbol.iterator)`, `sbom/ref`, and `signatures`; P0-018 explicitly says behavior must be identical and existing tests must not be weakened.

- `packages/catalog/test/catalog-entry.test.ts`: the new `assertRejectedByCatalogAndSafeNormalize` helper weakens those tests by asserting the first error is the root path and mirrors `safeNormalize.reason`. This makes the acceptance PASS dependent on removing prior path assertions. Under the rubric, this is a test-integrity failure.

- The refactor also drops the catalog normalizer’s prior `MAX_DEPTH = 64` / `MAX_ITEMS = 10_000` behavior and calls `safeNormalize(value)` with defaults of depth `100` and node budget `100_000`. That expands accepted input size, for example large `signatures` arrays above the old 10,000-item cap can now normalize and validate. P0-018 says no validation-rule changes and identical behavior.

**NOTES**

- The changed files stay within `packages/catalog`, and the §9.3 trust-tier / §13.1 reference checks appear otherwise unchanged.

VERDICT: reject