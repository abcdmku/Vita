**BLOCKING**
- [apply_linux.go](C:/Users/Borg/Documents/Vita/agent/capabilities/nodeconfig/apply_linux.go:98) can mutate the live config and still return an error that `transaction.Apply` cannot roll back. `AtomicWrite` renames the temp file into place at lines 98-100, then calls `os.Chmod` at lines 101-102. If that post-rename chmod fails, [nodeconfig.go](C:/Users/Borg/Documents/Vita/agent/capabilities/nodeconfig/nodeconfig.go:151) returns `nil, err`; [engine.go](C:/Users/Borg/Documents/Vita/agent/transaction/engine.go:158) treats the operation as not applied and only rolls back prior undos. Result: the plan reports rollback/failure while `node-config.env` may already contain the desired bytes. For an R3 transactional host capability, no error path after the commit point may leave an untracked mutation. Since the temp file is chmodded before rename, remove the post-rename chmod or restructure so any successful live swap returns an undo-capable success path.

**NOTES**
- Scope is tight: changed files are under `agent/`, and I do not see weakened/deleted/skipped tests.
- Tests cover apply/undo exact bytes, absent prior config, transaction commit, transaction rollback on later failure, invalid request rejection, and simulated pre-commit atomic-write failure. Add a regression for the post-commit error case above.
- `agent/cmd/agentd/main.go` still registers no capabilities. I am not marking this blocking because the required artifacts/tests instantiate `nodeconfig` directly, but if P1-007 intended runtime discoverability, that wiring is still missing.
- The reported acceptance command passed, but the contract definition of done also mentions `go vet ./...`; ensure the release gate runs it in the pinned container.

VERDICT: revise