**BLOCKING**

- `schema/capabilities/timesync.json` now models `desired.servers` as `hostnameRFC1123`, and `schema/capabilities/conformance/timesync.json` keeps IPv4/IPv6 literals as rejects. The unchanged Go `timesync.ApplyRequest` still accepts IP literals via its real decode/validate path, and this diff only adds real-entrypoint conformance for `node.config` in `agent/internal/capmanifest/nodeconfig_conformance_test.go`. There is no equivalent `transport.DecodeJSONRequest[timesync.ApplyRequest]` conformance, so the required manifest≡agent check for `time.sync` is missing and would currently disagree on the IP vectors. This violates the contract’s core R3 requirement.

**NOTES**

- I am not treating the shown `go` command failure on the Windows host as a code finding, per the prompt.
- The object manifest dialect shape itself is mostly consistent across TS/Go, but the time.sync real-entrypoint gap means this is not merge-ready.

VERDICT: revise