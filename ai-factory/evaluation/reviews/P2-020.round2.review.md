**BLOCKING**

- `controller/node-config/src/node-config-preview.ts:243` hard-codes `wideningInbound: false` when a `network` section is added. That means a valid change from “no network section” to a desired network config with non-empty `firewall.allow` is reported without the security flag. This violates the contract requirement to compose the section preview for sections present in either config and roll up `wideningInbound`.

**NOTES**

- The provided acceptance test passes, but it does not cover adding a network section that introduces inbound allow rules.
- Scope is limited to `controller/node-config`, with no new I/O, network, or privileged surface.

VERDICT: revise