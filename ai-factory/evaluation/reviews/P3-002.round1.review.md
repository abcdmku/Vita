BLOCKING
- `packages/runtime/src/sandbox.ts:178`: canonical `data` grants from the permission broker are not mapped. The broker’s `CapabilityGrant.scope` is the package volume name (`state`, `notes`, `index-cache`), not an absolute filesystem path (`runtime/permission-broker/src/decide.ts:335`, `runtime/permission-broker/test/decide.test.ts:8`). This implementation requires `scope` to pass `isScopedPath`, so real granted data capabilities are rejected instead of producing Deno `allowRead`/`allowWrite`. The tests use a synthetic absolute `data.scope`, so they miss the actual source-of-truth shape.

- `packages/runtime/src/sandbox.ts:31`: the module invents and accepts unbrokered grant vocabularies (`string` grants plus local `net`/`fs`/`env` object grants) that are not part of the imported `CapabilityGrant` model (`runtime/permission-broker/src/grants.ts:39`). That widens the enforcement boundary: a caller can pass `fs:write:/...` or `{ kind: "fs", ... }` and receive Deno permissions without the audited broker ever granting that capability. If `env`/`fs` grants are required, they need to be added to the canonical broker/source model first; `packages/runtime` should not silently create a parallel permission vocabulary.

NOTES
- The raw-input boundary does use `safeNormalize` before inspection, and the diff stays within `packages/runtime`.
- No existing tests appear weakened or skipped, but the new tests are overfit to the synthetic local grant shapes rather than proving behavior against real `CapabilityGrant[]` output from the broker.
- Consider adding regression coverage that feeds `denoSandboxPolicy(decideGrants(...).granted)` for an existing first-party-style package/policy.

VERDICT: revise