BLOCKING

- `sdk/typescript/src/capability-manifest.ts`: the diff intentionally narrows `TIMESYNC_MANIFEST` away from the contract’s required “hostname or IP-literal” rule. The added header text says ``timesync.servers` is intentionally scoped to RFC-1123 hostnames only` and `IP-literal ... support is deferred`, and the schema now uses only `pattern: RFC1123_HOSTNAME_PATTERN_SOURCE`. P9-002 requires mirroring timesync with IP-literal support; deferring it is an explicit contract deviation.

- `sdk/typescript/test/capability-manifest.test.ts`: test coverage was weakened to match the narrowed behavior. The required valid fixture was changed from `["pool.ntp.org", "10.0.0.1"]` to `["pool.ntp.org", "Time.Cloudflare.com"]`, the hostname/IP corpus test was deleted, and the new malformed-hostname loop asserts `"::1"` rejects. That removes proof of required IP-literal behavior and makes the acceptance lane pass despite the implementation no longer satisfying the task.

NOTES

- `canonicalizeHostname: patternSource === RFC1123_HOSTNAME_PATTERN_SOURCE` gives a raw regex string hidden normalization semantics that are not part of the declared dialect. After restoring IP-literal support, consider making canonicalization an explicit closed primitive rather than coupling it to one pattern literal.

VERDICT: revise