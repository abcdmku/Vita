BLOCKING

- `agent/internal/capmanifest/pdssync_conformance_test.go`: `TestPDSSyncFormatConformanceCorpus` validates the `cidV1Multibase` format corpus only through `Validate(manifest, ...)` and `normalizeStringFormat(...)`. That is manifest/self-oracle testing, not `pdssync.go` ground truth. The task explicitly required CID decode/structural parity against the real pdssync oracle; AGENTS.md trap 8 forbids self-referential conformance here. This matters because format-only vectors in `schema/capabilities/formats/pdssync-formats-conformance.json` cover codec/hash/digest-length cases that are not all exercised through `transport.DecodeJSONRequest[pdssync.ApplyRequest]+Validate()`.

NOTES

- I did not treat the reported `go` missing acceptance failure as a code blocker by itself.
- The manifest shape, capability name, registry addition, cursor float64-trunc option, and DID reuse look aligned with the contract from the diff.

VERDICT: revise