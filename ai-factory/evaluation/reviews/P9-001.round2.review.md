BLOCKING

1. `runtime/evaluator/src/evaluate.ts` hard-codes the capability vocabulary in `CAPABILITY_EVALUATORS` and locally redefines every request schema. This is still the prior-round contract violation: P9-001 requires the closed capability set and plan/operation/capability model to come from the existing SDK/source of truth, not a new evaluator-local list that can drift from `/apply`.

2. `runtime/evaluator/src/evaluate.ts` in `isIpv6AddressShape`, the final `return doubleColon.length === 2 || groups.length === 8;` accepts malformed IPv6 CIDRs such as `:::/0`. That lets `network.policy` produce an ok plan for a request the Go agent’s `netip.ParsePrefix` rejects, violating fail-closed malformed desired-state handling and the “ready to `/apply`” requirement.

3. `runtime/evaluator/src/evaluate.ts` in `isStaticTimestamp`, the zero-time check only rejects the exact `Z` spelling. `0001-01-01T00:00:00+00:00` is accepted by the evaluator but decodes to Go’s zero `time.Time` and is rejected by `time.set` validation. That is another evaluator-local schema drift that can emit a non-applyable plan.

NOTES

The added tests do not appear weakened or skipped, and the diff stays under `runtime/evaluator`. The main issue is that the tests only exercise happy-path examples and a few malformed shapes; they do not prove parity with the real agent request validators.

VERDICT: revise