**BLOCKING**

- `agent/capabilities/identity/identity.go:106`, `agent/capabilities/network/network.go:68`, `agent/capabilities/storage/storage.go:62`, `agent/capabilities/time/time.go:30`: the new `ApplyRequest.UnmarshalJSON` methods call `jsonsafe.Decode`, which wraps `json.Unmarshal` and ignores unknown fields. Because adding `UnmarshalJSON` bypasses the transport decoder’s existing `DisallowUnknownFields`, these capabilities now accept unknown top-level request fields and, for struct `desired` payloads, unknown nested fields that were previously rejected. That is a fail-open behavior change outside the contract’s “preserve existing behavior” scope. These should use `jsonsafe.DecodeStrict` or equivalent strict decoding.

**NOTES**

- The shared scanner itself is bounded by byte, depth, and node budgets, rejects top-level and nested duplicate keys, and stays within the allowed target paths.
- The added tests cover valid nested input, excessive depth, duplicate keys, oversized input, and representative capability decode paths, but they missed the strict unknown-field regression above.

VERDICT: revise