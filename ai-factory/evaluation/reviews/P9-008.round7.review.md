**BLOCKING**

- `agent/capabilities/nodeconfig/nodeconfig.go:303` introduces a recursive JSON pre-scan for duplicate keys, and the recursive calls at `agent/capabilities/nodeconfig/nodeconfig.go:331` / `:348` have no depth or node budget. This runs before the strict struct decode can reject unknown fields, so an untrusted `/apply` request like `{"extra":[[[...]]]}` can force deep recursion in privileged agent request decoding. R3 trust-boundary code must fail closed on deeply nested malformed input, not risk stack/CPU DoS. Add bounded depth/node accounting or reuse the bounded manifest JSON scanner pattern, and add a regression for excessive nesting.

**NOTES**

- The `timesync` default-registry exclusion and generated registry shape look aligned with the contract: default registry is `{ hostname.set, node.config }`, while `TIMESYNC_MANIFEST` remains exported with `defaultRegistry: false`.
- The `node.config` conformance path now uses `transport.DecodeJSONRequest[nodeconfig.ApplyRequest]`, which is the right entrypoint for the full `{"desired": ...}` request model.

VERDICT: revise