**BLOCKING**

- `agent/internal/capmanifest/capmanifest.go:1076-1078` breaks TS semantic parity for JSON numeric underflow. Example: with an integer field constrained to `minimum:0, maximum:0`, TS sees `{"count":1e-1000}` as `0` after `JSON.parse`, and `Number.isSafeInteger(0)` accepts it. This Go code rejects because `strconv.ParseFloat("1e-1000", 64)` returns `0` with `ErrRange`, and any non-nil error is rejected. The contract requires identical accept/reject decisions. Add a regression and handle JS-compatible underflow semantics, or explicitly align both planes.

- `agent/internal/capmanifest/capmanifest.go:30-32`, `:165-166`, `:908-915` hard-code the exact timesync hostname regex into the generic manifest validator. That is not dialect-driven validation: the package accepts this one JS-lookahead pattern via custom logic after Go regexp compilation fails, while any equivalent future manifest pattern fails closed. It also violates the contract’s “pattern via Go regexp with the SAME pattern string” requirement for the current manifest. This should be replaced with a generic, documented supported pattern subset/translator or a schema change through the proper contract, not an exact timesync special case in the generic TCB engine.

**NOTES**

- Orchestrator acceptance passed; I am not blocking on the worker’s Docker-access report.
- I did not see test weakening, skipped tests, `os/exec`, or edits outside the target paths.
- `capmanifest_test.go:224` reads the live schema by relative path rather than embedding/copying it as the contract suggested. That is non-blocking, but P9-006 should prefer `go:embed` or an explicit fixture for a tighter conformance handoff.

VERDICT: revise