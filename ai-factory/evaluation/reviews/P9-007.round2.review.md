BLOCKING

1. `schema/capabilities/hostname.json:10-12` adds `lowercaseOnly`, `singleLabel`, and `rejectAllNumeric`, and `agent/internal/capmanifest/capmanifest.go` / `sdk/typescript/src/capability-manifest.ts` extend the shared dialect to support them. The contract and ADR 0007 require named format primitives only for string structure, specifically `format:"hostnameRFC1123"` plus `maxLength` here. This is an ungoverned dialect expansion in a shared validation surface, not just a hostname manifest migration.

2. `schema/capabilities/conformance/hostname.json:165-167` does not prove manifest equivalence for duplicate JSON keys. The only duplicate-key vector has an invalid final value, so both validators reject for different reasons. With the Go path used at `agent/internal/capmanifest/hostname_conformance_test.go:95`, `{"desired":"bad_host","desired":"vita-node"}` is accepted by the agent’s `encoding/json` last-wins decode plus `Validate`, while the manifest validator rejects duplicate keys. That violates the required manifest≡agent-validator proof.

NOTES

The evaluator registry wiring to use manifest capability names (`time.sync`, `hostname.set`) looks aligned with the agent operation names and the contract’s default-registry requirement.

VERDICT: revise