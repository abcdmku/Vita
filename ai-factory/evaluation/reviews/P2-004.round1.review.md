BLOCKING

1. [controller/api/src/contracts.ts:163](/C:/Users/Borg/Documents/Vita/controller/api/src/contracts.ts:163) `IdentityRole` omits spec-defined roles. Spec §11 includes owner, administrator, member, restricted member, guest, and service roles; the new API type only allows `owner | admin | member | guest`. Because this is a typed controller contract and the task asks for “real and precise” identity summary types, the UI could not represent valid Vita roles. Add the missing role variants and cover them in the dispatcher test.

2. [controller/api/src/dispatcher.ts:821](/C:/Users/Borg/Documents/Vita/controller/api/src/dispatcher.ts:821), [controller/api/src/dispatcher.ts:960](/C:/Users/Borg/Documents/Vita/controller/api/src/dispatcher.ts:960) The new read-only endpoint param guards accept non-plain/exotic objects as valid empty params. `isRecord` returns true for instances like `new Date()`/`new Map()`/prototype-bearing objects, and the empty-param readers then accept them if `Object.keys` is empty. Under AGENTS.md trust-boundary rules and the contract’s malformed-params requirement, malformed params must fail closed with a typed error. Add regression coverage for exotic/malformed empty params and tighten the guard to only accept the documented empty plain shape.

NOTES

- Changed files are within `controller/api`, and I did not see skipped/deleted/weakened tests in the supplied diff.
- [controller/api/src/dispatcher.ts:128](/C:/Users/Borg/Documents/Vita/controller/api/src/dispatcher.ts:128) and [controller/api/src/dispatcher.ts:161](/C:/Users/Borg/Documents/Vita/controller/api/src/dispatcher.ts:161) return shared mutable stub objects despite readonly TypeScript types being erased at runtime. Consider freezing or returning fresh objects, especially for protection-state responses.

VERDICT: revise