**BLOCKING**

1. `packages/install/src/resolve.ts:253`: missing `requestedCapabilities` is treated as “grant every declared capability” via `capabilitiesFromContract(contract)`. That violates the contract’s default-deny / requested-capabilities model and AGENTS.md’s fail-closed rule for partial inputs. A verified entry that omits `requestedCapabilities` will receive all declared data, ingress, and egress grants without explicitly requesting them. The resolver should reject absent `requestedCapabilities` or grant none, not synthesize a full request. Add a regression for an otherwise valid entry with no `requestedCapabilities`.

**NOTES**

- The round-2 graph findings are addressed: roots come from npm root `dependencies` / JSR `specifiers`, unreachable lockfile records are excluded from the plan, and conflicting duplicate `name@version` records reject.
- `policyForTrustTier` still embeds tier policy locally. That matches the prior non-blocking note; centralizing later would reduce drift.
- The resolver only traverses `dependencies`, not optional/peer/dev dependency containers. The round-2 wording names root `dependencies`, so I am not blocking on it, but reviewers should confirm this is the intended install surface.

VERDICT: revise