**BLOCKING**
- `controller/changeset/src/changeset-preview.ts:256`: top-level `nodeConfig` add/remove is broken. Lines 246-247 set the absent side to `undefined`, then line 256 passes that into `previewNodeConfigChange`; the existing node-config preview rejects `undefined` as `INVALID_CURRENT_CONFIG`/`INVALID_DESIRED_CONFIG`. A valid change-set that newly manages or removes `nodeConfig` will return `{ ok:false }` instead of a `subsystems.nodeConfig.kind` of `"added"`/`"removed"` with the node-config flags. This violates the contract’s per-subsystem handling for all four subsystem keys.

**NOTES**
- The tests cover added services, removed capsules, aggregation, invalid input, and hostile envelopes, but they miss the top-level added/removed `nodeConfig` case that exposes the blocker.
- Diff scope is within `controller/changeset`; no test weakening or I/O/network behavior observed.

VERDICT: revise