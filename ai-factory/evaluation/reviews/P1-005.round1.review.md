BLOCKING

- `agent/hardware/discover_linux.go:265-292` / `agent/hardware/discover_linux.go:367-372`: PCI accelerator discovery emits `nvidia.cuda`, `intel.npu`, `amd.npu`, and `amd.rocm` objects without the fields required by the SDK/spec discriminated union (`memoryGB`, `compute`, `generation`). The contract says these accelerators must mirror the SDK kinds, and the TypeScript validator rejects missing required fields. This would produce an invalid capability snapshot for detected GPU/NPU hardware. Either populate the required fields from allowed sysfs/PCI data, or do not emit those non-CPU accelerator kinds until valid fields are available.

- `agent/hardware/discover_linux.go:30-38` / `agent/hardware/discover_linux.go:51-81`: `LinuxDiscoverer` exposes arbitrary path fields and `Discover` reads them directly. For an R3 privileged-agent component whose contract explicitly says discovery is read-only procfs/sysfs only, this broadens the read surface beyond the documented sources. Keep path injection unexported/test-only or constrain it to a fixed proc/sys root so production callers cannot turn this into an arbitrary file read primitive.

NOTES

- Tests cover CPU/memory parsing and a basic real `Discover` sanity path, but they do not catch the invalid accelerator payload shape above. Add deterministic sysfs/PCI fixture coverage for each emitted accelerator kind or a test proving GPU/NPU kinds are not emitted unless they are SDK-valid.

- I did not see shell execution, `os/exec`, or writes in the changed files, and the diff stays under `agent/`.

VERDICT: revise