**BLOCKING**
- `controller/capsule/test/capsule-preview.test.ts:208`: the method-shadowing regression is ineffective. `previewCapsuleChange` catches thrown exceptions and returns a rejection, so an implementation that actually calls `methodShadowedDesired.map()` would swallow this `assert.fail()` and the test would still pass. Add an external counter/assertion like the accessor and iterator cases so invocation is observable outside the caught exception. This matters because AGENTS.md requires proving untrusted method-shadowing inputs are not executed at the trust boundary.

**NOTES**
- The implementation otherwise appears scoped to `controller/capsule`, delegates validation to `validateCapsuleRegistry`, returns no diff on invalid input, and does no I/O/network.
- Version ordering is a local natural-string interpretation over a model that only validates non-empty strings. Non-blocking here because the contract does not define semver/catalog ordering, but this should be documented if callers will rely on precise upgrade/downgrade semantics.

VERDICT: revise