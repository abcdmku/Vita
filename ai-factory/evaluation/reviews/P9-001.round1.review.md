**BLOCKING**

1. `runtime/evaluator/src/evaluate.ts:153` / `:164` only require each capability value to be “some object”, then canonicalize it. That accepts structurally malformed agent requests such as `{ "hostname.set": {} }`, `{ "hostname.set": { "bogus": true } }`, or `{ "node.config": { "desired": { "mode": "normal" } } }` as successful plans. The contract requires malformed per-capability desired state to fail closed and produce no partial plan; this output is not ready for `/apply` because the Go transport’s typed request decoders/validators would reject it later.

2. `runtime/evaluator/src/evaluate.ts:220` / `:250` builds canonical objects with `{}` and assigns `output[key] = ...`. A normalized input containing an own `"__proto__"` key mutates the prototype of the generated request object instead of preserving or rejecting that data property. That reintroduces prototype pollution after `safeNormalize`, can silently drop attacker-controlled fields from the serialized plan, and violates the fail-closed/trusted-plain-data boundary.

3. `runtime/evaluator/src/evaluate.ts:21` / `:26` / `:31` locally redefines the capability, operation, and plan model instead of reusing the SDK/apply plan source of truth required by the contract. This creates a second closed capability list and a second operation type, so the evaluator can drift from the plan shape/capability vocabulary that downstream apply code consumes.

**NOTES**

The diff stays inside `runtime/evaluator`, and I do not see deleted, skipped, or weakened tests. The added tests cover the happy path, unknown top-level capability, one non-object malformed value, duplicate top-level JSON keys, and some hostile inputs, but they miss the request-schema and `__proto__` cases above.

VERDICT: revise