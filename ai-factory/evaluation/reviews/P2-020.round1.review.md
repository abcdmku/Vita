**BLOCKING**

- `controller/node-config/src/node-config-preview.ts:243` / `:257`: `previewStorageSection` returns `added`/`removed` directly when one side is absent, so it never runs the existing `previewStorageChange` for storage sections present in either config. That violates the contract and bypasses storage-preview-only fail-closed checks such as `DUPLICATE_SUBVOLUME_KEY`, which `validateNodeConfig` does not enforce. A newly added storage section with duplicate app-state role/path keys can therefore return `ok:true` instead of a typed section rejection.

**NOTES**

- Add a regression for the above: current without `storage`, desired with a storage layout that passes `validateNodeConfig` but is rejected by `previewStorageChange`; expected unified result is `ok:false`.
- No protected-path or acceptance-test weakening is visible in this diff.

VERDICT: revise