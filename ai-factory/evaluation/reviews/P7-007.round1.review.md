BLOCKING

- `agent/capabilities/services/services.go:503-513`: `validServiceName` only enforces length, separators, `..`, inline-material heuristics, and charset. It still accepts malformed non-systemd unit names such as `"."`, `".service"`, and `"ssh"`. The contract requires each `name` to be a valid systemd unit name, so this needs at least a recognized unit suffix and non-empty unit prefix validation.

- `agent/capabilities/services/services_test.go:323` and `agent/capabilities/services/services_test.go:493-501`: the commit-point regression is not actually injecting a failure. `failNextAtomicWriteAtCommitPoint` sets an observation flag after mutating `live`, but returns `nil`; rollback is then driven only by the later fake capability. This does not prove the required R3 partial-failure case for “failure at/after the effect” and leaves the critical test claim under-covered.

NOTES

- Diff stays inside `agent/capabilities/services/`.
- No `os/exec`, systemctl, network, or transport/cmd wiring was added.
- The default writer uses `os.CreateTemp` plus `Sync`, `Close`, and `Rename`, matching the intended atomic-write shape.

VERDICT: revise