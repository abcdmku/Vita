**BLOCKING**

- `schema/capabilities/formats/string-substrate-conformance.json:38` through `:55` only adds seed-related literal-token vectors (`seed-phrase`, `mnemonic`, `recovery_phrase`). The contract requires the frozen corpus to include a `seed-run` reject case, and the migration roadmap defines this class as 12-24 word seed runs. As written, the corpus can pass while missing that required inline-material class, so the R3 parity pin is incomplete.

**NOTES**

- The weak `noInlineSecrets` semantics appear to be replaced by a strong-scanner alias in the changed manifest code, which is acceptable for compatibility.
- I am not treating the provided `go` command failure as a blocking code finding because the failure shown is environmental rather than tied to a changed line.

VERDICT: revise