**BLOCKING**
- `controller/dashboard/src/node-dashboard.ts:412`: `clientErrorDetails()` is not fail-closed for malformed nested error details. It reads `agentError` from an injected `AgentClientError`, then calls `isRecord(agentError)`, whose implementation at `controller/dashboard/src/node-dashboard.ts:462` uses `Array.isArray(value)` without a guard. A revoked `Proxy` or similar exotic value in `agentError` can throw during error handling, causing `buildNodeDashboard()` to reject instead of returning a degraded section. This violates the contract’s “never throw/blank” fail-closed requirement and AGENTS.md trust-boundary rule. Make the nested detail probe non-throwing and add a regression test.

**NOTES**
- The diff stays within `controller/dashboard`.
- No weakened, skipped, or deleted tests are visible in the diff.
- Orchestrator acceptance passed for `node --experimental-strip-types --test controller/dashboard/test/node-dashboard.test.ts`.

VERDICT: revise