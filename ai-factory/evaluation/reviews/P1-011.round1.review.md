**BLOCKING**
- `os/x86_64/build-root.mjs:174-195` and `os/x86_64/mkosi.conf:1-10`: the executable mkosi plan does not actually consume `os/common/mkosi.conf`. The code validates both config texts and reports both digests, but the assembled command only runs `mkosi --directory /work/os/x86_64`; the arch config has no include/reference to the common layer. A real run would omit the Debian release/snapshot/mirror/package allowlist/SOURCE_DATE_EPOCH/network settings. The acceptance test misses this because it checks plan metadata, not that mkosi will consume both layers. Wire the common layer through a real mkosi-supported mechanism and test the executable args/config layering.
- `os/x86_64/build-root.mjs:84`: `ghcr.io/systemd/mkosi:v25.3` is a mutable tag, not an immutable pinned container input. The contract explicitly requires a pinned container and reproducible build inputs. Pin by digest or an equivalent immutable local image reference, and assert that in the plan/test.

**NOTES**
- Diff stays within `os/`; no protected policy or evaluation tests were changed.
- The malformed/accessor input regression is useful, but `npm run typecheck` currently excludes `os/**/*.ts`, so the new TS test/declaration files are not covered by the repo’s normal typecheck lane.
- `build-root.d.mts` is extra surface beyond the required artifacts; acceptable if kept, but it should stay synchronized with the `.mjs` implementation.

VERDICT: revise