BLOCKING

- `controller/api/src/dispatcher.ts:708-714`: migration readiness incorrectly lets architecture-neutral runtimes bypass the package contract’s declared architecture support. A valid `ts-service` or `wasm-component` capsule with `manifest.package.architectures: ["x86_64"]`, neutral state, and target `arm64` will return `migratable: true` because `packageReady` is `neutralRuntimeReady || architectureDeclared(...)`. The product spec says supported packages declare architectures, so an explicit x86-only contract must not preview as arm64-migratable. Require `architectureDeclared(..., targetArch)` separately from runtime artifact readiness, and add a regression for TS/WASM x86-only targeting arm64.

NOTES

- I did not see weakened, skipped, or deleted tests in the provided diff; the test changes are additive.
- The controller-local plain-input snapshotter is large and duplicates patterns already present in capsule/simulation/broker validators. It appears aimed at the round-2 TOCTOU requirement, but it is a maintainability concern for future controller endpoints.
- The response exposes both `granted`/`denied` and `grantedCapabilities`/`deniedCapabilities`; not blocking, but it adds API redundancy.

VERDICT: revise