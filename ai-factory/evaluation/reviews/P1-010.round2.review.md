**BLOCKING**

- `agent/capabilities/hostname/hostname.go:210-217`: `Apply` still performs irreversible `SetKernelHostname(target.name)` before the fallible `/etc/hostname` commit. If `staged.Commit()` fails and the compensating `SetKernelHostname(current.name)` also fails, the code returns `PartialApplyError` with the live kernel hostname mutated and no undo recorded by `transaction.Apply`. That is exactly the forbidden third state for this R3 contract: error with live system changed. This also violates the round-2 “single commit point / no fallible step after the privileged effect” requirement.

- `agent/capabilities/hostname/hostname_test.go`: the partial-failure regression is too weak. `TestCommitFailureAfterKernelSetRestoresPriorHostname` only covers the happy compensation path where file commit fails before file mutation and kernel restore succeeds. It does not test the explicit `PartialApplyError` branch or a commit facade that reports failure after applying its file effect, so the required “no untracked mutation on partial failure” property is not proven.

**NOTES**

- No evidence of test deletion/skipping or scope creep outside `agent/capabilities/hostname/`.
- `set_linux.go` correctly avoids `os/exec` and routes hostname mutation through `agent/internal/sysdeps.SetHostname`.
- Consider preserving an existing `/etc/hostname` mode of `0000`; `StageHostnameFile` currently treats mode `0` as “use default 0644,” which is a minor exact-restore gap.

VERDICT: revise