BLOCKING

- `sdk/typescript/src/capability-manifest.ts:1839` makes the TS `rfc3339Instant` parser looser than the Go agent. For a value like `2026-06-21T1x:02:03Z`, `parseOneOrTwoAsciiDigits` returns hour `1`, then this line advances two characters because the second character is not `:`, landing on the later colon and accepting the malformed timestamp. Go’s `time.Parse(time.RFC3339, ...)` rejects that shape. This breaks the contract’s manifest≡agent requirement and lets the evaluator accept a `time.set` request the agent will reject. Track the consumed hour width explicitly, or require the second character to be `:` when only one digit was consumed, and add corpus vectors for this case.

NOTES

- The intentional Go quirks in the corpus, including comma fractions, one-digit hours, and `24:60` offsets, appear consistent with the current stdlib fallback behavior, so I am not flagging those.
- The supplied acceptance run was not green because `go` was missing from PATH, so the Definition of Done is still not evidenced by that run, but this is not a diff-line blocking finding by itself.

VERDICT: revise