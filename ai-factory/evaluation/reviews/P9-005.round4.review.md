**BLOCKING**
- `agent/internal/capmanifest/capmanifest.go` in `validateStringField`, at the added `normalized = strings.ToLower(raw)` line: Go lowercasing is not JS-compatible. JS `"\u0130".toLowerCase()` expands to `i` + combining dot, while Go’s `strings.ToLower` uses simple rune mapping. With a valid dialect manifest using `lowercase: true` plus `enum` or `uniqueItems`, Go and TS can make different accept/reject decisions. The contract requires TS/Go decision parity.
- `sdk/typescript/test/capability-manifest.test.ts` in `TIMESYNC rejects malformed hostnames and out-of-scope IP literals`: the diff deletes the existing `tooLongHostname` rejection case. That weakens an existing test and violates the contract requirement to keep prior assertions under the new `format` shape.

**NOTES**
- The diff appears confined to the declared target paths.
- The supplied Go acceptance run passed, and the committed manifest/generated const do replace `pattern` with `format`.

VERDICT: revise