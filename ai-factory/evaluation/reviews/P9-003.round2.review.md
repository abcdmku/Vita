**BLOCKING**

1. `TIMESYNC_MANIFEST` is still a hand-maintained inline manifest, not sourced from `schema/capabilities/timesync.json`. The contract requires the JSON artifact to become the single source of truth and the public `TIMESYNC_MANIFEST` to be obtained from it. In the diff, [sdk/typescript/src/capability-manifest.ts](sdk/typescript/src/capability-manifest.ts) still defines `export const TIMESYNC_MANIFEST = Object.freeze({ ... })` around the hunk adding `version: SUPPORTED_MANIFEST_VERSION`; the new test only compares the JSON-loaded manifest to this inline constant (`sdk/typescript/test/capability-manifest-loader.test.ts:23`). That proves parity at test time but leaves two authoritative definitions in production, so future TS/Go drift is still possible.

**NOTES**

- The loader itself is generally fail-closed and uses `safeNormalize`, including hostile accessor/cycle coverage.
- Diff stays within the allowed target paths, and I do not see test weakening or skipped tests.

VERDICT: revise