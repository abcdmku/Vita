**BLOCKING**

- `simulation/harness/src/simulate.ts:689-692`, `simulation/harness/src/simulate.ts:910-913`, `simulation/harness/src/simulate.ts:935-938`: profile-side unhealthy storage is ignored. `projectProfile` grants storage capacity/capabilities from any `profile.storageLayout`, while `rejectStorageIssues` checks only `operation.layout.diskHealth` for `critical` / SMART `failed` and uses `profileStorageLayout` only for free bytes. A storage-op with a healthy plan layout can therefore succeed against a profile whose declared storage layout is critical or failed. That violates the contract’s requirement to evaluate operations against the profile’s declared resources/capabilities and the P0-019 storage feasibility dependency. Add a profile-storage-health rejection and a regression test.

**NOTES**

- The diff stays inside `simulation/harness` and does not weaken existing tests.
- The implementation is large for this contract, which raises review/maintenance cost, but I did not find a separate scope or security blocker beyond the storage feasibility bug.

VERDICT: revise