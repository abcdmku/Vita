**BLOCKING**
- `controller/dashboard/src/node-dashboard.ts:105` uses `states.status` as evidence that the node is reachable. But `readStates` returns `status: "ready"` for a valid empty `capabilities` list at `controller/dashboard/src/node-dashboard.ts:236-239` without making any `getState` call, and returns `status: "degraded"` for locally malformed capabilities at `controller/dashboard/src/node-dashboard.ts:218`, also without reaching the agent. A fully unreachable client with `capabilities = []` would report `reachable: true`, violating P2-016’s requirement that a fully unreachable client returns `reachable: false`.

**NOTES**
- No weakened/skipped tests detected.
- Acceptance covers fully unreachable only with a non-empty capability list, so it misses the reachable false edge above.

VERDICT: revise