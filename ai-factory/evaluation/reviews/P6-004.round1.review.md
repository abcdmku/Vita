BLOCKING

1. [agent/capabilities/capsule/capsule.go:41](C:\Users\Borg\Documents\Vita\agent\capabilities\capsule\capsule.go:41), [agent/capabilities/capsule/capsule.go:432](C:\Users\Borg\Documents\Vita\agent\capabilities\capsule\capsule.go:432) — `Registry.Capsules` is a non-pointer slice and `normalizeRegistry` treats `nil` as an explicit empty list. That means typed input like `ApplyRequest{Desired:&Registry{}}` passes `Validate()`/`Apply()` and writes `{"capsules":[]}`. The JSON `UnmarshalJSON` path rejects missing `capsules`, but the typed request validation path remains fail-open for an incomplete required field. This violates the contract/AGENTS.md absent-required-field rule; use an explicit presence model while still accepting `{"capsules":[]}`.

1. [agent/capabilities/capsule/capsule.go:412](C:\Users\Borg\Documents\Vita\agent\capabilities\capsule\capsule.go:412), [agent/capabilities/capsule/capsule.go:499](C:\Users\Borg\Documents\Vita\agent\capabilities\capsule\capsule.go:499) — `validVersion` allows `:` via `versionPattern` and does not reject inline schemes, so a registry entry with `version:"data:QUJD"` passes and is persisted. The contract requires embedded capsule bytes be rejected and the registry store reference/integrity metadata only. Add rejection coverage for inline/data/literal schemes in `version` or otherwise constrain versions so inline capsule content cannot be stored in an allowed scalar field.

NOTES

Acceptance was reported passing for `node tools/build/go-in-docker.mjs --dir agent test ./...`. No existing tests appear weakened or skipped, and the diff stays under `agent/capabilities/capsule/`. The added tests cover many JSON-path failures, but they miss the typed nil-capsules case and inline-data-in-version case above.

VERDICT: revise