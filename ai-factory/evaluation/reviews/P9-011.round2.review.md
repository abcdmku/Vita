**BLOCKING (must fix before merge)**

- `schema/capabilities/timesync.json:15` makes `servers.items` use `format: "hostnameOrIp"`, but that format is stricter than the real timesync agent for malformed numeric dotted quads. Example: `{"desired":{"enabled":true,"servers":["1.2.3.999"]}}`. The agent tries `netip.ParseAddr`, then falls back to `validHostname`, which accepts four numeric hostname labels; the manifest format rejects all numeric dotted quads after IP parse fails. This violates the contract’s manifest≡agent requirement and creates preview/evaluator rejection for input the agent apply validator accepts. The new conformance corpus does not include this edge, so the manifest≡agent test misses the divergence.

**NOTES (non-blocking)**

- I did not treat the shown Windows `go` missing failure as a code finding by itself.
- The nested `crossFieldRules` extension is directionally scoped to the contract and appears closed rather than adding an expression surface.

VERDICT: revise