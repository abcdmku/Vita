**BLOCKING**

- `agent/internal/auditlog/auditlog.go:81-82` and `:234-239`: the production filesystem path uses `os.WriteFile` against the predictable temp name `s.path + ".tmp"` before `Rename`. That is not the proven P1-007 atomic-write pattern for this R3 provenance store. A pre-existing temp entry, especially a symlink/hardlink in the state directory, can cause the temp write/truncate to affect something other than a fresh temp file before the commit point, and the code also skips the write/sync/close preparation used by neighboring agent atomic stores. The contract requires the whole log to persist through a single temp+rename commit point. Use a fresh exclusive temp file in the target directory, set restrictive mode, write/sync/close it, then rename, with no fallible work after rename.

**NOTES**

- Scope is otherwise tight: only `agent/internal/auditlog` is touched, no transport wiring, no `os/exec`, and no existing tests are weakened.
- The SDK audit-event wire shape is mirrored via the nested `actor` object, and the new tests cover monotonic assignment, canonical bytes, duplicate-key/non-canonical rejection, log-full behavior, invalid-event rejection, key-material rejection, and same-store concurrent appends.

VERDICT: revise