BLOCKING

- `sdk/typescript/src/storage-model.ts:572` and `sdk/typescript/src/storage-model.ts:834`: `subvolumes` only has to be non-empty and duplicate-free. This accepts incomplete layouts missing the spec/ADR storage areas (`system state`, `user data`, `app state`, `snapshots`, `local backup cache`). For a fail-closed validator of the §10 Btrfs subvolume layout, partial layouts must be rejected, not treated as valid.
- `sdk/typescript/src/storage-model.ts:304`: `appId` is optional and not tied to `role`. An `app-state` subvolume can be anonymous, and non-app roles can carry `appId`. That does not satisfy §10.2 “app-specific subvolumes” as a deterministic controller/agent contract. Add role/appId invariants and regression tests.

NOTES

- Scope is clean: only the two target files are changed, and I see no weakened, skipped, or deleted tests in the diff.
- The `safeNormalize` trust-boundary use is directionally correct and avoids re-walking hostile input.
- `sdk/typescript/src/storage-model.ts:729` only checks `path.startsWith("/")`; before this feeds privileged storage code, consider canonical path/prefix restrictions.

VERDICT: revise