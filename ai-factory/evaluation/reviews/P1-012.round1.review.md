**BLOCKING**
- `os/x86_64/uki.mjs:106-119`: the executable UKI assembly plan does not actually make the generated `os-release` input available to `ukify`. The plan records `inputs.osRelease.text`, but the command passes `--os-release /work/os/x86_64/uki.os-release`; no such artifact is created or mounted, and `/work` is read-only. This is the same correctness class that blocked P1-011: metadata validates an input while the runnable plan would drop or fail to consume it. Materialize this input in the plan using a real ukify-supported mechanism and test that the command consumes it.
- `os/x86_64/uki.mjs:121-130`: the signing step is presented as an executable `docker ukify sign` command, but it passes abstract `test:` references as private key/certificate arguments and has no declared mechanism for resolving TEST-only signing material. The contract permits TEST key references and forbids embedded production material, but the plan still needs a coherent test-signing contract: either model this as a non-executable owner/test signing capability, or provide a deterministic TEST artifact reference/path flow that keeps material out of the repo.

**NOTES**
- Diff stays within `os/`; no protected policy, hidden eval, signing/provenance, or existing tests are modified.
- The digest pins, `SOURCE_DATE_EPOCH`, no-network declarations, and TEST-only production-signing separation are covered by the added test.
- The input guard covers partial and accessor-bearing objects; broader hostile-shape coverage would be useful but is secondary to the broken executable plan semantics above.
VERDICT: revise