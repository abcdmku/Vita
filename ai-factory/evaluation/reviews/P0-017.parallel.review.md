**BLOCKING**
- `storage/capsules/src/capsule.ts:1189`: The branch still reimplements the trust-boundary traversal after `safeNormalize` fails (`addNormalizationError` -> `findNormalizationDiagnostic` -> `inspectUnsafeValue`/array/object helpers). P0-017 explicitly required `safeNormalize(input); if (!n.ok) return reject;` and removal of duplicated accessor/exotic/cyclic/method-shadow handling. This keeps a second unaudited traversal over untrusted input, contrary to the objective and AGENTS “snapshot once / operate only on trusted data” rule.

- `storage/capsules/src/capsule.ts:303`: `safeNormalize` is called with `maxNodes: MAX_ITEMS + 1`, changing the old validator’s limit from per-object/per-array `MAX_ITEMS` to a whole-tree node budget. A capsule with, for example, 10,000 valid signature refs was previously within the per-array limit but can now fail normalization once earlier object nodes are counted. The contract says no validation-rule or behavior changes.

**NOTES**
- I do not see test weakening or protected-file edits in the supplied diff.
- §13.1 reference/signature validation appears unchanged by this patch.
- Acceptance passing is not enough here because the existing tests assert legacy paths but do not cover the large-capsule budget behavior or the contract requirement to remove duplicate normalization logic.

VERDICT: revise