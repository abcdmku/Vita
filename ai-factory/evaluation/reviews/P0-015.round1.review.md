**BLOCKING**
- `sdk/typescript/src/safe-normalize.ts` and `sdk/typescript/test/safe-normalize.test.ts` are not strict-TypeScript clean under the repo baseline. `tsconfig.base.json` enables `noUncheckedIndexedAccess`; examples include unchecked `keys[keyIndex]`, `snapshot.entries[index]`, `cases[index]`, `source.nested.labels[1].score`, and `value[keys[index]]`. The acceptance command strips types, so it misses this, but AGENTS.md requires strict TS compliance before merge.

- `sdk/typescript/src/safe-normalize.ts:86` can still throw from its `catch` block. If an unexpected throw has a hostile `message` getter, or `opts` has a getter that throws such an `Error`, `error instanceof Error ? error.message : ...` can throw to the caller. The contract explicitly says `safeNormalize` must never throw and any thrown error must become `{ ok:false }`. The catch path should not read attacker-controlled exception properties without its own fail-closed guard.

- `sdk/typescript/src/safe-normalize.ts:357` snapshots every object key into `entries` before enforcing the node budget on children. A very wide plain object causes unbounded descriptor walking and a second large allocation even when `maxNodes` should reject early. For a trust-boundary primitive, the budget must be enforced while snapshotting, with a regression test for wide objects / small `maxNodes`.

**NOTES**
- The visible tests cover the named adversarial cases reasonably well, but the Proxy case uses an empty proxy handler. Add a hostile proxy trap regression to prove traps are not invoked before rejection.
- Scope is within `sdk/typescript`, and I do not see test weakening or protected-path edits.

VERDICT: revise