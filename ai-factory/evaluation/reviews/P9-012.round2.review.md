**BLOCKING**

- `absolutePath` rejects `/`, contrary to P9-012’s rule “no trailing slash (unless root).” The changed validators reject root at [agent/internal/capmanifest/capmanifest.go:1513] and [sdk/typescript/src/capability-manifest.ts:1954]. The shared conformance corpus was also flipped from `"absolutePath root accepts"` to `"absolutePath root rejects"` at [schema/capabilities/formats/structured-formats-conformance.json:384-387], so both planes now agree on the wrong contract behavior. Restore `/` as an accepted canonical absolute path in TS + Go and restore the corpus expectation.

**NOTES**

- I am not blocking solely on the supplied acceptance run failing because `go` was unavailable on that runner. The code issue above is independent of that environment failure.
- The diff stays within the contract target paths. I did not find another changed-line issue that rises to blocking.

VERDICT: revise