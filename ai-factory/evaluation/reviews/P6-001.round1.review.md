**BLOCKING**
- `storage/capsules/src/capsule.ts` lets embedded material pass in several legitimate reference fields. `runtime.ociImageIndexes[].ref`, `runtime.wasmComponents[].ref`, `runtime.typescript.source/compiled.ref`, and `state.exports[].ref` are all modeled as refs but validated with `validateRequiredString`, not `validateRequiredReference`. A value like `-----BEGIN PRIVATE KEY-----\n...` would validate there, violating §13.1 and P6-001’s “secrets-as-references enforced” requirement. The test only covers an extra `privateKey` field, so it misses embedded material placed in allowed `ref` fields.

- `storage/capsules/src/capsule.ts` accepts `manifest.signatures: []`. Spec §13.1 says capsules are signed, and the contract requires manifest signatures as references. Structurally accepting a capsule with no signature reference does not meet that. Add a non-empty array check for signatures and a regression test.

**NOTES**
- Diff stays within `storage/capsules`; no protected files or acceptance tests were modified.
- No test weakening/skipping/gaming observed, but coverage should include embedded secrets in allowed reference fields, not only unknown secret-named fields.
- `TypeScriptRuntimeArtifacts` has both `source` and `compiled` optional at the type level even though the validator requires at least one. Consider modeling it as a union so the exported `Capsule` type matches the runtime contract.

VERDICT: revise