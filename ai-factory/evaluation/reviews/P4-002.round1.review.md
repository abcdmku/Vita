BLOCKING

- `packages/catalog/src/lockfile-policy.ts:455` / `packages/catalog/src/lockfile-policy.ts:563`: nested dependency declarations are only checked for exact version strings, not for a corresponding resolved entry with an integrity hash. A lockfile like `{ dependencies: { a: { version: "1.0.0", integrity: "sha512-a", dependencies: { b: "2.0.0" } } } }` appears to pass even though `b` has no resolved lockfile entry or integrity. That violates the contract’s core requirement that every dependency be pinned to an exact version plus integrity hash, and it is a supply-chain gate bypass.

- `packages/catalog/src/lockfile-policy.ts:506`: lifecycle-script indicator fields are rejected only when the value is exactly `true`. Malformed or ambiguous values such as `requiresBuild: "true"` or `hasInstallScript: 1` are accepted instead of being rejected fail-closed. For a supply-chain validator, known lifecycle-script signal fields with non-boolean or unexpected shapes are not proof of script-free input.

NOTES

- The acceptance tests cover the listed happy path and several direct violations, and I do not see weakened or skipped existing tests. They are missing the transitive/unresolved dependency regression above.
- The integrity check accepts placeholder-looking digests such as `sha512-leftpad`; if this validator is meant to enforce real SRI/hash syntax rather than just algorithm-prefixed presence, tighten this before relying on it as a hard supply-chain gate.
- The implementation is large for a new standalone validator and recognizes many undocumented shapes. That is maintainable only if the accepted lockfile schema is clarified with tests.

VERDICT: revise