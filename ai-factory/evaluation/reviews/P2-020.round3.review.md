BLOCKING

- `controller/node-config/src/node-config-preview.ts:181`, `:360-363`, `:373`: removed backup sections can fail to surface `weakensRetention`. The synthetic absent backup baseline has `retentionDays: 1`, and the unified preview copies `preview.weakensRetention`. If current backup retention is already `1`, removing the backup section compares `1 -> 1` and reports `weakensRetention: false`, even though removing backup retention is a weakening. This violates the contract’s requirement to faithfully surface the backup security flag for whole-node changes.

- `controller/node-config/src/node-config-preview.ts:109-190`, `:303-306`, `:332-335`, `:360-363`: added/removed section diffs are polluted by fake baseline resources. For example, an added network section without `lo` reports removal of the synthetic `lo` interface; added storage reports removals of `/absent/...` subvolumes; added backup reports changes from a fake schedule/target. The operator-facing unified diff is therefore not a correct section diff for added/removed sections, even though summary status is correct.

NOTES

- The new hostile-input test is not a strong accessor regression because `previewNodeConfigChange` catches thrown errors; the `assert.fail` inside the getter could be swallowed and still produce a passing rejection.

VERDICT: revise