**BLOCKING**

1. `controller/apply/src/apply-flow.ts:154` can still throw while converting client/apply failures. `errorMessage()` reads `error.message` directly from an arbitrary thrown `Error`; a hostile injected client can throw an `Error` instance with a throwing `message` accessor, causing `applyConfig()` to throw instead of returning a typed flow error. The contract requires client/apply errors to be typed and “never a throw,” and AGENTS.md requires unknown trust-boundary inputs to fail closed.

2. `controller/apply/src/apply-flow.ts:66` submits the original `plan` object after previewing it. Because `previewPlan()` snapshots/normalizes for its own validation but does not return a canonical plan, the object can be mutated between the awaited preview and `client.apply(plan)`. That can submit operations different from those previewed, violating the fail-closed preview-then-apply contract.

**NOTES**

The diff stays within `controller/apply`, composes the existing agent client and preview module, and the visible acceptance tests cover the happy path, unknown operation rejection, preview discovery errors, apply errors, and rolled-back results. No test weakening is visible in this diff.

VERDICT: revise