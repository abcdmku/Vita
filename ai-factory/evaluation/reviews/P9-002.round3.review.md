BLOCKING

- `sdk/typescript/src/capability-manifest.ts:1284` (`address = value.slice(0, lastColon);`) breaks valid compressed IPv6 literals with dotted IPv4 tails. For example, `2001:db8::192.0.2.1` is split to `2001:db8:` before parsing, so it is rejected even though it is a valid IP literal and should mirror the agent’s `netip.ParseAddr` behavior. This also affects `::192.0.2.1`. The task requires the timesync manifest to accept hostname or IP literals and stay aligned with the agent rules; this is a correctness regression in the changed code. Add coverage for compressed dotted-tail IPv6 and preserve the `::` when splitting the embedded IPv4 tail.

NOTES

- No test weakening or out-of-scope file changes are visible in the provided diff.
- The added canonicalization tests cover IPv4-mapped forms, but not the dotted-tail compression case above.

VERDICT: revise