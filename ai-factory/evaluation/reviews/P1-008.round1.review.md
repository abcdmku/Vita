**BLOCKING**

- [agent/transport/server.go:84](agent/transport/server.go:84) / [agent/cmd/agentd/main.go:37](agent/cmd/agentd/main.go:37): `JSONRequestDecoder[nodeconfig.ApplyRequest]()` only performs strict JSON decoding; it does not reject incomplete typed requests. For example, `{"desired":{"mode":"normal"}}` decodes successfully with `remoteAccess == ""`, then reaches `transaction.Apply`. The failure occurs inside `nodeconfig.Apply`, so the handler returns a rolled-back transaction result instead of rejecting the bad request as a typed 4xx before any transaction work. Worse, if a partial invalid operation follows a valid operation, the valid operation can be applied and then rolled back. AGENTS.md requires partial/incomplete trust-boundary input to be rejected fail-closed, not partially honored. Add pre-transaction validation for the full typed request shape and a regression proving missing required fields apply nothing.

**NOTES**

- The loopback bind is correctly constrained to `127.0.0.1:8786`, and I do not see `os/exec`, shell expansion, or public listener scope creep.
- The changed test file is additive; I do not see weakened, skipped, deleted, or gamed existing tests in the supplied diff.
- The shown orchestrator lane proves `go test ./...`; the contract also asks for `go vet ./...`, so integration should still run that gate even though I am not making it a separate blocker here.

VERDICT: revise