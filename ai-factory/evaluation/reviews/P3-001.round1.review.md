**BLOCKING**

1. `packages/install/src/resolve.ts:330`, `packages/install/src/resolve.ts:396`, `packages/install/src/resolve.ts:671`: the resolver does not resolve the reachable dependency graph for the entry/root. NPM root dependencies in `packages[""]` are skipped, JSR `specifiers` are ignored, and `orderedSteps` visits every record in the lockfile. A policy-valid lockfile with an unrelated pinned package will produce a plan that installs extra code outside the package dependency set. That violates the closed graph / least-supply-chain-surface requirement. Fix by deriving roots from the npm root package and JSR specifiers, then traversing only that closure, or reject unreferenced records.

2. `packages/install/src/resolve.ts:858`: duplicate `name@version` records are silently coalesced. `upsertStep` keeps the first integrity/source and unions dependencies without checking for conflicts. A lockfile with two entries for the same package/version but different SRI/source/dependencies can pass policy and yield a fabricated or under-specified plan. For a pinned install plan this must reject conflicting duplicates, or preserve path-qualified identities.

**NOTES**

- Tests cover the happy path, policy rejection, one out-of-tier network request, and hostile normalization, but miss the graph-root, unreferenced-entry, duplicate-conflict, and JSR success cases above.
- `policyForTrustTier` embeds new trust-tier policy locally. If that is intended, it should eventually be centralized/shared to avoid drift from the broker/catalog policy surface.

VERDICT: revise