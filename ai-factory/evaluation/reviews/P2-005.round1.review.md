BLOCKING
- `controller/api/src/dispatcher.ts:1299-1323`, plus reads at `532`, `550`, `571`, `593`: the new endpoint does not fail-closed on the top-level `{ capsule, targetArch, policy }` boundary object. `readCapsuleImportPreviewParams` checks `Object.keys`/`hasOwnProperty` and then returns the original object; it also reads `params.targetArch` directly. Plain objects with accessor properties are accepted, so a caller can execute getters and return different `capsule`/`policy` values across the validation and grant-decision reads. This violates the task’s explicit malformed/exotic fail-closed requirement and AGENTS.md trust-boundary rule. Normalize/reject the params object using data-property descriptor checks before any field access, and pass trusted values forward. Add a regression for accessor/getter params, not only nested malformed capsule/policy values.

NOTES
- `controller/api/src/dispatcher.ts:849-890`: migration readiness short-circuits to `migratable:true` for architecture-neutral state without checking target runtime support, and otherwise only checks OCI index platforms. If FR-020 is meant to preview moving the whole app plus state, this may over-report readiness for neutral-state, x86-only OCI capsules and under-specify TS/WASM behavior. The contract wording is a bit ambiguous, so I’m not making this blocking, but it should be clarified.
- No test weakening or protected-path edits are visible in the supplied diff; changes stay under `controller/api`.

VERDICT: revise