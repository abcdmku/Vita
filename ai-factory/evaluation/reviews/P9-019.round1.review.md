BLOCKING

- `agent/internal/capmanifest/capmanifest.go:2183` and `sdk/typescript/src/capability-manifest.ts:3443` implement the identity secret scanner by ASCII-lowercasing and then doing literal token scans. The oracle in `agent/capabilities/identity/identity.go` uses `(?i)` Go regexes for the identity scanner, which use Unicode case folding. That makes the manifest/SDK scanner looser than the agent for folded secret tokens; for example a URI key ref containing `apiKey=` is rejected by the agent’s `api[-_\s]?key\s*(=|:)` scanner but is accepted by this port as a valid `keyReference`. This violates the contract’s manifest≡agent requirement for the identity secret scanner.

NOTES

- The visible identity corpus also does not cover this class of scanner parity case; add frozen vectors against the real identity entrypoint when fixing.
- I am not treating the reported Windows `go` executable failure as a blocking code finding by itself, per the review instructions.

VERDICT: revise