BLOCKING

- `controller/accounts/src/accounts-preview.ts:220-222`, with writes at `:236`, `:245`, and `:257`: the diff records are plain `{}` objects keyed by account name. A model-valid account name like `__proto__` mutates the record prototype instead of creating an own enumerable key. That means a valid added/removed/modified account can disappear from `diff`, and `newlyEnabledCount` / `groupChanges` can be wrong because they later use `Object.keys(...)`. Use null-prototype records or another safe keyed structure, and add a regression for a valid `__proto__` account.

NOTES

- The implementation otherwise stays in scope, imports `validateAccountsConfig`, avoids I/O/network, and the provided tests cover the main contract cases.
- The hostile input behavior relies on the SDK validator’s `safeNormalize`, which is the right shared boundary for this task.

VERDICT: revise