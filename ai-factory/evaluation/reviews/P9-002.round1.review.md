**BLOCKING**

- `sdk/typescript/src/capability-manifest.ts:93-107` hand-rolls a reduced IP literal regex for `TIMESYNC_SERVER_PATTERN`. The Go timesync capability accepts `netip.ParseAddr` before hostname validation, so valid IP literals such as IPv4-embedded IPv6 (`::ffff:192.0.2.1`) are accepted by the agent but rejected by this manifest. That violates the contract’s requirement to mirror the agent timesync constraints and creates manifest/agent conformance drift.

- `sdk/typescript/src/capability-manifest.ts:91-92` under-detects inline secret material for `noInlineSecrets`: `DATA_URI_PATTERN` only matches `data:` at the beginning of the string, and `LONG_BASE64_PATTERN` misses base64url-style long blobs. The contract requires `noInlineSecrets` to reject PEM / `data:` / long-base64 material; as written, allowed string fields can still carry inline data content.

**NOTES**

- The diff stays within the two target files and appears additive.
- No weakened, skipped, deleted, or gamed tests are visible in this diff.
- The returned timesync `value` preserves input spelling rather than agent-style canonicalized host/IP output; I am not treating that as blocking because the contract is framed around validation, but it is worth documenting before this becomes a shared source of truth.

VERDICT: revise