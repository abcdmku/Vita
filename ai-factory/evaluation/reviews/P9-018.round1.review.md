**BLOCKING**

None.

**NOTES**

The accounts manifest tracks the real `agent/capabilities/accounts` validator: exact operation name, UID bounds, shell allowlist, required `enabled`, required non-null `groups`, privileged group rejection, group dedupe, and independent `name`/`uid` uniqueness.

The new `notInEnum`, `dedupItems`, and independent multi-field `uniqueBy` behavior is covered in both TS and Go primitive tests, plus accounts conformance. The Go conformance test uses the real `transport.DecodeJSONRequest[accounts.ApplyRequest]` + `Validate()` oracle.

The supplied acceptance transcript failed before running tests because `go` was unavailable. Per the prompt, I did not treat that environment failure as a diff-level blocker; the merge gate should still rerun the pinned Go container lane.

VERDICT: approve