BLOCKING

1. `runtime/permission-broker/src/decide.ts:482` accepts malformed manifest-shaped data policy/declarations and can still grant from them. `normalizeDataRequirements` does not require `classes`, treats missing `volumes` as empty, and `normalizeDataVolume` only checks `name/class/access`, ignoring required `DataVolumeRequirement` fields like `mountPath`, `persistence`, `backup`, and `sizeMiB`. A runtime input typed as `DataRequirements` but missing required fields can authorize a grant. That violates the contract’s fail-closed requirement for malformed/unknown input and the DoD requirement to reuse package-contract declarations without weakening their shape.

2. `runtime/permission-broker/src/decide.ts:554` accepts partial `NetworkPolicy` objects and can grant from them. `NetworkPolicy` requires both `ingress` and `egress`, but `normalizeNetworkPolicy` grants whichever side is present. A malformed destination policy such as `{ network: { egress: [...] } }` can explicitly allow egress instead of being rejected as malformed and all-denied. This is a default-deny/security bug in the broker decision core.

3. `runtime/permission-broker/src/decide.ts:191` accepts an undocumented `package` alias as a package declaration. `CapabilityRequest` only models `packageContract`, but `readPackageDeclaration` will grant based on `request.package`, including when `packageContract` is absent or unreadable. That widens the trust-boundary API beyond the typed contract and conflicts with “unknown/ambiguous => deny.”

4. `runtime/permission-broker/test/decide.test.ts` does not prove the malformed-input fail-closed cases above. The fail-closed test only uses wholly malformed inputs/policies; it does not check malformed-but-partially-usable package declarations or destination policies that currently still grant. For this R2 broker change, those regressions must be covered before merge.

NOTES

- The implementation is very large for the requested pure decision core, which hurts the spec/rubric comprehension bar for a TCB component. Tightening shape validation by reusing or mirroring the P1 manifest validator more directly would make the grant rules easier to audit.
- No evidence of test weakening/deletion in the supplied diff; the issue is missing adversarial coverage, not gamed existing tests.

VERDICT: revise