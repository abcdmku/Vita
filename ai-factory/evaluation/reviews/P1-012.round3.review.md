BLOCKING

- `os/x86_64/uki.conf:7`, `:10`, `:13` use obvious placeholder digests (`sha256:111...`, `222...`, `333...`) for kernel, initrd, and stub. That does not satisfy the contract’s “digest-pinned” external preconditions; it only satisfies the test regex. A runner enforcing these would either reject real artifacts or treat fake pins as proof. Replace with real upstream artifact digest metadata or model the plan as unresolved until real digests are supplied, and make the test reject placeholder/sentinel digests.

NOTES

- The round-3 signing-env blocker is addressed: the config is locked to `VITA_TEST_*` env names, signing is non-executable, and no production key material is embedded.
- The os-release dangling-path issue is materially improved by declaring it as a produced input consumed by the assembly command.

VERDICT: revise