**BLOCKING**

- `os/x86_64/image.conf` / `SlotRootA`, `SlotRootB`, `SlotRecovery`: the plan treats the same P1-012 UKI (`/external/p1-012/vita-x86_64.unsigned.efi`) as the boot artifact for root-A, root-B, and recovery, but the existing P1-012 UKI cmdline in `os/x86_64/uki.conf` is fixed to `root=LABEL=vita-debian-trixie-x86_64-root`. That is not coherent with this layout’s root labels (`vita-root-a`, `vita-root-b`) or the recovery slot. As planned, the ESP targets look slot-specific, but the boot artifact cannot select the corresponding slot. The test only checks that `uki` is referenced, not that the consumed UKI can boot any declared slot. Fix by modeling slot-compatible boot artifacts/cmdlines, or by making the layout labels/root selection match the consumed UKI without ambiguity.

- `os/x86_64/image.conf` / `[Inputs]`: `RootImagePath=/external/p1-011/vita-debian-trixie-x86_64-root.img` does not match the accepted P1-011 scaffold, which declares mkosi `Format=directory` and `Output=vita-debian-trixie-x86_64-root`, not a `.img` artifact. The plan says this is the P1-011 upstream output, and the tests hard-code that invented path, so the external precondition can be “satisfied” for an artifact P1-011 does not define. Either reference the actual upstream artifact shape or explicitly model a separate root-partition-image conversion precondition.

**NOTES**

- Diff stays within `os/`; no protected policy files or existing tests were weakened.
- The canonical path, unresolved digest, sentinel rejection, and accessor-input regressions follow the hardened P1-012 pattern.
- The RAUC bundle object is still an abstract plan, not a real RAUC manifest; that is acceptable for this scaffold, but the next RAUC contract should validate slot-class semantics against actual RAUC manifest/system.conf output.

VERDICT: revise