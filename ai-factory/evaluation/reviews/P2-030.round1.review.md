BLOCKING

- `controller/apply-node/src/apply-node-config.ts:74` / `controller/apply-node/src/apply-node-config.ts:198`: the apply-result parser rejects every optional field except `error`. Current Go `/apply` can return a committed 200 result with `auditUnrecorded: true` when audit append fails or no audit store is configured. This code would submit the validated plan, let the agent commit, then report `{ ok:false, stage:"apply" }` as a malformed response because of that field. That violates the success path and creates a dangerous committed-but-reported-failed controller outcome. Add support for the current wire field and a regression test.

NOTES

- `controller/apply-node/src/apply-node-config.ts:159`: rolled-back 200 results are treated as `ok:true`. That may be intended if `ok` only means the agent returned a typed result, but the contract only names committed success and rejected apply failure. I would clarify this before relying on the API semantics.

VERDICT: revise