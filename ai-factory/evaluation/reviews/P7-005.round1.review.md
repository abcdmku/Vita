**BLOCKING**
- `agent/internal/auditlog/auditlog.go:123` / `:137` / `:165` - `Append` is a read-modify-rewrite with no serialization or compare-and-swap around the fixed log path. Two concurrent appends can both load the same live log, both assign the same next sequence, and both return success after `AtomicWriteFile`; the later rename silently overwrites the earlier event. That violates the contract’s append-only / strictly monotonic / never-drop-history requirement for the provenance store. This needs a lock or equivalent atomic update protocol covering load, sequence assignment, validation of current state, and rename for the fixed path.

**NOTES**
- The diff stays inside `agent/internal/auditlog`, uses no network or `os/exec`, and the supplied acceptance lane passed.
- The JSON canonicalization, duplicate-key rejection, log-full typed error, caller-sequence override, and secret-material rejection are covered by new tests.

VERDICT: revise