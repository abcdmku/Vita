BLOCKING

- `sdk/typescript/src/plan.ts`: the exported `isCanonicalPlan(value: unknown)` is not fail-closed for arbitrary malformed inputs. A cyclic `config` object recurses through `isCanonicalJsonValue` until it throws `RangeError` instead of returning `false`. Since P0-014 explicitly makes this the shared boundary guard over `unknown` and requires fail-closed behavior, this should be fixed before merge. Add cycle-safe traversal or catch-and-return-false inside `isCanonicalPlan`, and add a regression test.

NOTES

- Scope is otherwise tight: changed files stay inside `sdk/typescript` and `controller/api`.
- The envelope duplication is removed in the supplied diff, and dispatcher no longer uses the unchecked `currentPlan as CanonicalPlan` cast.
- Added tests cover the contract’s listed basic malformed cases, but they should include the fail-closed malformed-object case above.

VERDICT: revise