**BLOCKING**

- [os/x86_64/uki.mjs:582]( /dev/null ) `assertContainerPath()` is only a string-prefix check. A config such as `Kernel.Path=/external/../plan/generated/uki.os-release` or `UKI.Output=/out/../work/x` passes validation, is emitted directly into `ukifyArgs` at lines 113-124, and can escape the declared mount root after path normalization by the container/tool. That breaks the round-3 requirement that kernel/initrd/stub are external verified preconditions and undermines the “no dangling/only declared artifacts” path model. Fix by canonicalizing/validating POSIX container paths and rejecting `.`/`..`, empty segments, duplicate slashes, control chars, and paths outside the exact allowed mount root; add traversal regression tests.

**NOTES**

- The unresolved digest model matches the round-4 escape hatch, and the current config correctly blocks assembly while unresolved.
- Signing is modeled as non-executable TEST-only metadata with fixed `VITA_TEST_*` env names and no embedded key material.
- The test suite is new and no existing tests appear weakened or skipped.

VERDICT: revise