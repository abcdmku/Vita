**BLOCKING**

- `controller/api/src/dispatcher.ts` `previewAppInstall` treats `decision.denied.length === 0` as success, but `decideGrants` reports malformed policy by denying each requested capability. If the package contract is valid but requests zero capabilities, for example empty `data.volumes`, `network.ingress`, and `network.egress`, `capabilitiesFromPackageContract()` returns `[]`; a malformed/partial policy then produces no denials and the endpoint returns `ok: true`. The contract requires malformed input to return a typed error and fail closed, so partial node policy can be silently accepted.

- `controller/api/src/dispatcher.ts` `clonePlainArray()` reads `value.length` from untrusted array/proxy input outside a catch, and `previewAppInstall()` has no outer catch. A hostile proxy around an array can throw from the `length` getter and make the API method throw instead of returning an `AppInstallPreviewResponse`. This violates the contract and AGENTS.md trust-boundary rule that malformed input never throws; the added tests cover method shadowing but not hostile getters/proxies at the controller boundary.

**NOTES**

- The capability extraction helper duplicates broker/package declaration semantics in the controller. It is mostly consistent with the current broker, but it is another place that must be updated if broker capability semantics expand.

- I did not see test weakening or edits outside the declared `controller/api` target paths in the supplied diff.

VERDICT: revise