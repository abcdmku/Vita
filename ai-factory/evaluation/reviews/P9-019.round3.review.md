**BLOCKING**
- `sdk/typescript/src/capability-manifest.ts:~3120` added `const scheme = value.slice(0, separator).toLowerCase();`, but this is not equivalent to the Go oracle’s `strings.ToLower` for all Unicode scheme prefixes. Go lowercases `\u0130` to ASCII `i`, so `İ://ref` is accepted by `identity.validKeyRef`; JavaScript lowercases it to `i\u0307`, causing `isURIReferenceScheme` to reject it. That makes the TS manifest stricter than the agent for `keyReference`, violating the contract’s manifest≡agent requirement. Use a Go-compatible simple lowercase routine for scheme normalization and add a lock-in vector such as `"\u0130://ref"`.

**NOTES**
- I did not treat the reported Go acceptance failure as blocking because the prompt says not to block solely on that lane’s host PATH issue.
- No weakened or skipped tests were apparent in the provided diff.

VERDICT: revise