**BLOCKING**

- `sdk/typescript/src/capability-manifest.ts:1150` returns `value.toLowerCase()` for IPv6 literals instead of the agent’s canonical `netip.ParseAddr(value).String()` behavior. Because uniqueness is checked after this normalization, inputs like `["2001:0db8::1", "2001:db8::1"]` are accepted as distinct here but rejected by the Go timesync capability as duplicates. That violates the contract’s requirement that `TIMESYNC_MANIFEST` mirror the real timesync rules.

**NOTES**

- Tests are additive and do not weaken existing coverage. Add a regression for canonical-equivalent IP duplicates, especially IPv6 zero-padding and IPv4-mapped forms.
- `sdk/typescript/src/capability-manifest.ts:10` explicitly says the implementation “approximates” Go `netip` parity. That is a useful caveat for an exploratory note, but not acceptable for this contract’s concrete conformance PoC.

VERDICT: revise