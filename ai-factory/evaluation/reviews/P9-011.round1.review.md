**BLOCKING**

1. `schema/capabilities/timesync.json` now places `"noInlineSecrets": true` on `desired.servers.items`, while `agent/internal/capmanifest/conformance_test.go` now requires manifest decisions to match the real `transport.DecodeJSONRequest[timesync.ApplyRequest]` + `Validate()` path. The unchanged agent accepts syntactically valid hostnames such as `pool.AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA.org`; the manifest rejects them via `noInlineSecrets`. The diff also removes the shared conformance vector `"inline base64ish secret rejects"` from `schema/capabilities/conformance/timesync.json`, so the new agent-vs-manifest gate no longer exposes this disagreement. That violates the contract’s manifest≡agent requirement and is a test-integrity regression.

**NOTES**

The reported acceptance failure is environmental (`go` not found on this host), so I am not treating that alone as a blocking code finding. The blocking issue above is from the diff itself.

VERDICT: revise