**BLOCKING**

1. `node.config` manifest shape violates the contract. The task explicitly required top-level fields `mode` and `remoteAccess` using the existing string/enum dialect. The diff instead makes the only top-level field `desired` at `schema/capabilities/node.config.json:5` and adds `"type": "object"` at `:7`; the generated export mirrors this at `sdk/typescript/src/generated/capability-manifests.generated.ts:23` and `:43`. This rejects the contracted request shape and implements a new dialect the contract forbids.

2. The diff expands the shared manifest dialect with object fields even though P9-008 says “enums only” and “no new format.” The new TS object schema is introduced at `sdk/typescript/src/capability-manifest.ts:57-69`; the Go validator adds nested object fields at `agent/internal/capmanifest/capmanifest.go:43-44` and object schema parsing at `:120-125`. That is substantial shared validator surface outside the task.

3. `time.sync` is rewritten out of scope. The contract is for `node.config`; it does not authorize changing another capability’s manifest/API. `schema/capabilities/timesync.json:5-8` now wraps the existing fields in `desired`, and the generated `TIMESYNC_MANIFEST` does the same at `sdk/typescript/src/generated/capability-manifests.generated.ts:53-87`. This changes existing request shape and rejection paths for a non-target capability.

4. Test integrity issue in the modified `time.sync` conformance corpus: the former IP-literal rejection coverage is removed/replaced. In current `schema/capabilities/conformance/timesync.json`, the vector at `:98-106` is now case-insensitive duplicate coverage where the diff previously had the IPv4 literal reject case, and the IPv6 reject vector is also gone. That weakens documented manifest behavior while this diff simultaneously adds time.sync agent-vs-manifest conformance.

**NOTES**

- The nodeconfig duplicate-key hardening direction is reasonable, but it should be kept only to the extent needed for the contracted validator parity after the schema shape is corrected.
- The orchestrator’s shown acceptance pass only proves the rewritten nodeconfig corpus matches the rewritten TS validator; it does not make the contract/schema drift acceptable.

VERDICT: revise