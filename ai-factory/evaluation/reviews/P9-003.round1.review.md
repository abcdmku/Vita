BLOCKING

- `sdk/typescript/src/capability-manifest.ts:1`, `:19`, `:222`, `:261-263` add production import-time JSON loading via Node `createRequire`. The contract’s security/perf constraint says to read JSON via Node fs in the test only and keep `loadCapabilityManifest(raw)` operating on parsed data. This also makes the SDK module do hidden filesystem I/O on import, which conflicts with the deterministic/no-I/O TypeScript planning surface in the product spec. Rework so production import of the loader stays pure, while preserving JSON equivalence/authority through an explicit load/build/test path.

NOTES

- `sdk/typescript/src/capability-manifest.ts:737`, `:778-790` freeze the rules array but not individual cross-field rule objects. The old inline manifest froze each rule object; this is a mutability regression for exported `TIMESYNC_MANIFEST`.
- No weakened, skipped, or deleted tests are visible in this diff. The new tests cover the main malformed-manifest and behavior-parity cases.

VERDICT: revise