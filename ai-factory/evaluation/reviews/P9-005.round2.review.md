**BLOCKING**

1. `schema/capabilities/timesync.json:14`, `sdk/typescript/src/capability-manifest.ts:22/197/928`, and the matching Go `rejectIpv4Form` handling expand the manifest dialect to make the Go implementation work. P9-005 requires a Go counterpart to the existing TS validator/shared dialect; the allowed string schema keys in the contract do not include `rejectIpv4Form`, and the DoD says the engine is additive. This changes the source of truth instead of mirroring it.

2. `agent/internal/capmanifest/capmanifest.go` compiles manifest regexes with Go `regexp.Compile(source)` and validates with `MatchString`, while the TS validator still uses JS `new RegExp(source, "u")`. The added compatibility filter only rejects some constructs; it still allows patterns whose semantics differ between Go and JS, such as `$`, `.`, and `\s`. That violates the contract’s generic “semantic parity” requirement for same manifest + same request.

**NOTES**

Go acceptance is reported passing. I did not see deleted or skipped tests in the supplied diff, but the TS/schema changes are not covered by the reported Go-only acceptance lane.

VERDICT: revise