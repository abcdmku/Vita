# Vita system agent state directory — P1-026.
# agentd persists per-capability state + the append-only audit log under this root and expects
# it to exist as 0700 root:root at boot (StateDirectory= also creates it, but tmpfiles guarantees
# the mode/owner deterministically even on a pre-existing tree).
d /var/lib/vita-agent 0700 root root -
