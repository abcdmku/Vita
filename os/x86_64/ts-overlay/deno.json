{
  "//": "Deno config for the ON-DEVICE TypeScript overlay (P1-030). This file makes `deno check` the",
  "//2": "authoritative typechecker for the production entrypoint that runs on-device under the pinned",
  "//3": "Deno runtime. The Node `tsc` lane (root tsconfig.json) cannot validate this code — it uses the",
  "//4": "Deno.* global namespace, not Node types — so coverage of the entrypoint lives HERE, checked",
  "//5": "against Deno's real lib, exactly as it will run on the device. See the acceptance command in",
  "//6": "os/x86_64/ts-image.conf [Typecheck] and the README note below.",
  "//7": "Pure on-device control-plane code: no permissions, no remote imports (only vendored relative",
  "//8": "imports under usr/lib/vita/ts/). lint/fmt kept on Deno defaults; strict typecheck is the point.",
  "lint": {
    "rules": {
      "tags": ["recommended"]
    }
  },
  "compilerOptions": {
    "strict": true,
    "noUncheckedIndexedAccess": true,
    "exactOptionalPropertyTypes": true,
    "noImplicitOverride": true,
    "noFallthroughCasesInSwitch": true
  }
}
