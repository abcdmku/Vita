import * as React from 'react';
/** Home-screen app icon (Lucide) with a label beneath. */
export interface HomeTileProps { icon?: string; label?: string; accent?: boolean; size?: number; }
export function HomeTile(props: HomeTileProps): JSX.Element;
