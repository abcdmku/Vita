import * as React from 'react';
/** Lock-screen clock — light-weight, oversized. */
export interface LockClockProps { time?: string; date?: string; }
export function LockClock(props: LockClockProps): JSX.Element;
