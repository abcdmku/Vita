import * as React from 'react';
/** Control Center toggle tile (Lucide icon). Accent-filled when on. */
export interface ControlCenterTileProps { icon?: string; label?: string; value?: string; on?: boolean; wide?: boolean; }
export function ControlCenterTile(props: ControlCenterTileProps): JSX.Element;
