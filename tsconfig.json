{
  "//": "Type-check lane (spec §20.1 'TypeScript stable compile'). Checks all first-party TS against",
  "//2": "the strict baseline WITHOUT emitting — `npm run typecheck`. The acceptance tests run via",
  "//3": "node --experimental-strip-types which ERASES types, so this is what actually enforces strict TS.",
  "extends": "./tsconfig.base.json",
  "compilerOptions": {
    "noEmit": true,
    "allowImportingTsExtensions": true,
    "types": ["node"]
  },
  "include": [
    "sdk/**/*.ts",
    "controller/**/*.ts",
    "runtime/**/*.ts",
    "packages/**/*.ts",
    "protocols/**/*.ts",
    "storage/**/*.ts",
    "simulation/**/*.ts",
    "accelerators/**/*.ts",
    "os/**/*.ts",
    "agent/**/*.ts"
  ],
  "//4": "ts-overlay/** is ON-DEVICE Deno code (uses Deno.* globals, not Node) staged into the rootfs",
  "//5": "and run by the pinned Deno runtime on-device (P1-030). It is intentionally NOT part of this",
  "//6": "Node typecheck lane; Deno type-checks it via `deno check` (see os/x86_64/ts-image.conf).",
  "exclude": ["node_modules", ".vita-worktrees", "**/dist", "os/x86_64/ts-overlay/**"]
}
