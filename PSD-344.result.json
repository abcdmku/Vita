{
  "id": "PSD-344",
  "status": "done",
  "acceptance": {
    "command": "npm run typecheck && node --experimental-strip-types --test sdk/typescript/test/ui-kits/files-backend.test.ts",
    "exitCode": 0,
    "result": "pass",
    "output": "> vita@0.0.0 typecheck\n> tsc --noEmit -p tsconfig.json\n\nTAP version 13\n# Subtest: requestFile maps list/read/write/stat through the agentd files responder\nok 1 - requestFile maps list/read/write/stat through the agentd files responder\n# Subtest: requestFile returns FilesErrorResponse for oversize and agentd denials\nok 2 - requestFile returns FilesErrorResponse for oversize and agentd denials\n# Subtest: requestFile enforces bridge grants fail-closed before agentd transport\nok 3 - requestFile enforces bridge grants fail-closed before agentd transport\n# Subtest: requestFile rejects fractional agentd sizes across response shapes\nok 4 - requestFile rejects fractional agentd sizes across response shapes\n1..4\n# tests 4\n# suites 0\n# pass 4\n# fail 0\n# cancelled 0\n# skipped 0\n# todo 0"
  },
  "notes": [
    "PowerShell on this host rejected the literal && separator, so the acceptance command was executed via cmd /c.",
    "The default npm/node shim resolves through a sandbox-denied nvm symlink; the passing run used a temporary npm runner under the session temp directory with an accessible Node 22 binary on PATH."
  ]
}
